---
status: confirmed
layer: feature-design
task: SYS-HUB-FOUNDATION-P11
parent_feature: feat-hub-foundation
feature_package_id: feature-package/feat-hub-foundation
feature_context_digest: sha256:938ecf38d145496bba7a439b829d3934718b8f43b4f4628d8ba821594d17062d
package_digest: sha256:8735bb1680e29f961a3e76fc33b07944368946f486875f20e2ce77007c81b502
collected_at: "2026-07-24"
---

# 再検証証跡 (2026-07-24) — ブロッカーの原因確定と G9/G11 の実測

> 2026-07-21 の初回証跡収集で「未確認」「未取得」だった項目を再検証した記録。`gh` の認証が回復したことで GitHub 側の状態が確認可能になった。

## 1. GitHub Secrets / Variables の確定（従来「未確認」）

| 検証 | 結果 |
|---|---|
| `gh auth status` | 回復（daishiman / repo・workflow scope あり） |
| `gh secret list --repo daishiman/HarnessHub` | **空（1 件も未設定）** |
| `gh variable list --repo daishiman/HarnessHub` | **空** |

→ release-notes §2-5 の「未確認（以前の記録では未設定）」を **未設定と確定**。

## 2. main CI（A1）の失敗原因の確定

| run | trigger | 結果 |
|---|---|---|
| 29819387390 | PR #21 merge push (2026-07-21) | 静的ゲート ✓ → build & test ✓ → **wrangler deploy ✗** |
| 29978594614 | PR #45 merge push (2026-07-23) | 同上（deploy のみ ✗） |

deploy job のログ実測（run 29978594614）:

```
✘ [ERROR] In a non-interactive environment, it's necessary to set a
  CLOUDFLARE_API_TOKEN environment variable for wrangler to work.
```

→ **A1 blocked の根本原因は `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` secret と `HUB_HEALTH_URL` variable の未設定**（§1 で全て空と確定）。test→deploy の連鎖自体は設計どおり単一 run 内で発火している（deploy job は起動している = R-02 是正の構造は機能）。

- 再実行コマンド: `gh run view <run-id> --repo daishiman/HarnessHub --log-failed`

## 3. backup.yml の状態

日次 schedule は 3 日連続で発火し（30032772879 / 29945372343 / 29856488673）、**必須 secret 5 件（TURSO_AUTH_TOKEN / TURSO_DATABASE_NAME / R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY / R2_ACCOUNT_ID）不在の事前検査で fail-closed に停止**している。設計どおりの赤（silent skip ではない）。

## 4. cron トリガー登録失敗のエラーコード確定（従来「エラーコード未取得」）

`WRANGLER_LOG=debug WRANGLER_LOG_SANITIZE=false pnpm exec wrangler triggers deploy`（apps/hub、2026-07-24）で API レスポンス本文を取得:

```
PUT /accounts/b3dde7be.../workers/scripts/harness-hub/schedules → 400
{
  "code": 10072,
  "message": "You have exceeded the limit of 5 cron triggers. ..."
}
```

→ release-notes §3 の仮説 1（**アカウント単位の cron 上限 Free 5 本に到達**）が**実測で確定**。解消はユーザー判断（他プロジェクトの cron 削除 / Hub cron の統合 + 空き枠確保 / 有料プラン）。

## 5. G11 CWV 初回実測（従来「未取得」）

`gh workflow run cwv.yml -f target_url=https://harness-hub.daishimanju.workers.dev` → run **30074457529**:

| 指標 | 実測 | 予算 | 判定 |
|---|---|---|---|
| LCP | 1154.16 ms | 2500 ms | OK |
| CLS | 0.00 | 0.1 | OK |
| TBT (INP の lab 代理指標) | **926.50 ms** | 200 ms | **NG** |

→ run は fail 終了 = **G11 が劣化を検出して赤くなることを実証**（ゲートの実効性確認）。CWV good（qa-018）自体は**未達**であり、不要 JS 削減の是正を起票する。未計測を good と読み替えていた状態は解消。

## 6. G9 axe 違反注入の両系化（従来「片系のみ」）

画面結合側 `apps/hub/tests/a11y/hub-screens.spec.ts` に alt 無し画像の注入テストを恒久追加し、`image-alt` 違反の検出を固定。

| 系 | テスト | 結果 |
|---|---|---|
| 部品単体 (packages/ui) | `src/a11y/axe.test.tsx`（既存） | 30 tests pass |
| 画面結合 (apps/hub) | `tests/a11y/hub-screens.spec.ts`（注入テスト追加後） | 3 tests pass |

- 再実行コマンド: `pnpm --filter @harness-hub/ui run test:a11y && pnpm --filter @harness-hub/hub run test:a11y`
