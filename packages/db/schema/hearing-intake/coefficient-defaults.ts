/**
 * tenant_coefficients の既定値。テナントが設定を保存するまでこの値で試算する。
 *
 * ここを唯一の出所にしている理由は、同じ数値が過去に 3 箇所へ独立に書かれていたため
 * (テーブル定義の default・repository の取得時フォールバック・入力画面の参考値)。
 * 業務側が「35% を 40% にしたい」と言ったときに、どれか 1 つだけ直して
 * 画面と保存値が食い違う、という壊れ方をしていた。
 *
 * 発行済みの migration SQL にも同じ数値が焼かれているが、そちらは適用履歴なので変更しない。
 * この定数を変えるときは、既存テナント行の更新 (migration の追加) が別途必要になる。
 */
export const DEFAULT_TENANT_COEFFICIENT_VALUES = {
  /** 1 人あたりの年間労働時間。時給の換算に使う */
  annualHours: 2_000,
  /** 1 回の作業にかかる分数の既定 */
  minutesPerRun: 15,
  /** 今の工数のうち、どれだけ減らせると見込むかの割合 (0.35 なら 35%) */
  sheetReductionRate: 0.35,
} as const;
