{
  "artifact": "plugins/dev-graph/scripts/render-graph-html.py",
  "type": "scripts",
  "mechanical": true,
  "llm_eval": {
    "verdict": "PASS",
    "score": 90,
    "notes": "genuine code-review + py_compile OK / frontmatter 完備 (network:false, write-scope: argv --out only)。task graph を依存フリーな静的 HTML/SVG に決定的レンダリング。入力 node を正規化し dangling depends_on / parent_feature を fail-closed で reject、feature の子 done/total 進捗を集計 (feature_progress aggregate+by_feature)。registration receipt は既存 validator で parent/count/node IDs/graph digest/source digest を照合し、成功時だけ registration_verification=verified、未指定時は not_performed とする。同じ state を CLI JSON、HTML banner、render-metadata へ投影するため表示間の意味が一致する。XSS 防御: 全補間テキストに html.escape、埋込 JSON payload は '<' を \\\\u003c へ置換。出力は mkstemp→fsync→os.replace の原子書込、source==out を拒否。input_sha256/output_sha256 と node/edge 数を receipt 出力する。SMELL: document 全体が巨大 f-string で密、フィルタ JS は n.className.baseVal 参照 (SVG 前提で妥当)。ContractError→exit1。TESTS: test_render_registration_verification.py の receipt 有無の正負対照、test_sync_render_schedule_v2.py、test_runtime_coverage.py が verification state・hash・進捗・不正入力拒否を exercise。"
  },
  "reviewed_by": "workflow-code-review",
  "checked": ["soundness", "responsibility", "error-handling", "side-effect-safety", "frontmatter"]
}
