---
graph_node_id: "arch-system-spec-overview"
artifact_kind: "architecture"
artifact_subtypes: ["backend","data","security"]
project_id: "system-spec-import"
domain: "system-spec"
tags: ["system-spec","source-lineage","imported"]
priority: null
start_date: null
target_date: null
iteration: null
title: "system-spec architecture overview"
owners: ["system-spec-harness"]
created_at: "2026-08-17T08:41:48Z"
updated_at: "2026-08-17T08:41:48Z"
status: "active"
depends_on: []
related_nodes: []
resource_scope: ["system-spec/00-requirements-definition.md","system-spec/completeness-report.json"]
purpose: "確定済み system-spec の architecture context を参照可能にする。"
goal: "仕様由来の architecture context を feature から参照できる状態。"
scope_in: ["confirmed system-spec requirements artifact"]
scope_out: ["confirmed artifacts are not rewritten by this adapter"]
acceptance: ["source lineage と evaluator evidence を保持する","C02 でのみ登録する"]
architecture_refs: []
parent_feature: null
feature_package_id: null
phase_ref: null
file_path: "architecture/system-spec-overview.md"
template_id: "architecture"
template_version: "1.0.0"
confirmation_status: "confirmed"
evaluation_status: "pass"
confirmation_evidence: {"evaluated_digest":"7ed726cf030c9db0f1a9175068c1aea5879c32bc5d152337ae23eb485f85426b","evaluator":"system-spec-harness/assign-system-spec-completeness-evaluator","evidence_ref":"system-spec/completeness-report.json"}
source_lineage: {"imported_at":"2026-08-17T08:41:48Z","origin_kind":"system-spec-harness","source_digest":"ce4e67a9460878b7d8a1c471f85d8114678e4f9d890a85860d83be5d7ae99b8a","source_path":"system-spec/00-requirements-definition.md","source_plugin":"system-spec-harness","source_version":"0.1.0"}
classification_confidence: 1.0
classification_reason: "system-spec-harness が確定した architecture context の import。"
classification_candidates: [{"artifact_kind":"architecture","candidate_path":"architecture/system-spec-overview.md","confidence":1.0}]
issue_linkage: null
tracker_binding: "none"
beads_linkage: null
github_publication: {"labels":[],"milestone":null,"mode":"local_only","project_aliases":[]}
github_project_linkages: []
pull_request_linkages: []
execution_contexts: []
completion_evidence: {"completed_at":"2026-08-17T08:41:48Z","evidence_refs":["system-spec/completeness-report.json"],"policy":"manual","reconciled_at":"2026-08-17T08:41:48Z","source":"manual","status":"done"}
implementation_readiness: {"checked_at":"2026-08-17T08:41:48Z","missing_sections":[],"status":"complete"}
---

# 要件定義書 (上位概念)

> 本章は spec-state.json の requirements_foundation を正本とする、システム構築の憲法。
> 以降の各技術章は frontmatter の serves_goals でここ (ゴール) へトレース (anchor) する。
> 上位概念がブレなければ、仕様が整った後もブレない。

- 確定マーカー: `status: confirmed`

## U1 本質的目的 (essential_purpose)

1 つの Hub に複数の顧客企業 Workspace を同居させ、事業として横展開できる基盤を持つこと。非エンジニアの業務当事者が自分の業務課題を AI (Claude Code / Codex) と自分で解決し、Git や公開工程の複雑さを意識せず社内へ届けられる状態は、この事業展開を成立させるための手段として位置付ける。提供者の手離れも同じく手段であり、それ自体を目的にしない。 (appr-062 / qa-296。appr-001 の『非エンジニアの自己解決の実現を目的とし、手離れと収益はその帰結』から中立再確認により変更した)

## U2 背景 (background)

提供者が shadow publish 的な代理作業で公開を肩代わりしており、公開数に比例して提供者の時間が消える構造に限界を実感した。ユーザーは解決物を「作れる」のに、Git・PR・CI を伴う公開・共有の基盤がないことがボトルネックになっている (doc/harness-hub-platform-concept.md §2.2 の障壁認識。同認識は仮説 H0 として Stage 0 で再検証する)。

## U3 ゴール (goals)

| ID | ゴール |
|---|---|
| G1 | 作者の自己完結 publish: 非エンジニアの作者が、提供者の代理作業なし・Git 操作ゼロで公開・更新・rollback を自走できる |
| G2 | 統制と安全性の担保: Workspace 管理者が承認・監査・公開停止を行え、shadow IT 化せず統制点が一元化される |
| G3 | 既存資産の再利用による最小工数: harness-creator の package check / package contract / marketplace catalog / version・cache 処理 / review workflow を移植元の正本として再利用し、二重実装を避けて構築する |
| G4 | 複数顧客への同時展開基盤: マルチテナント論理分離を Stage 1 から実装し、1 つの Hub に複数顧客 Workspace をデータ・権限・Catalog の境界を保ったまま同居させる |
| G5 | 導入されたハーネスの利用実態と削減効果 (時間・金額換算) が可視化され、フィードバック→AI 対応→再公開の改善ループが定着する |
| G6 | 公開された業務ツールを owner 以外の同僚が見つけて追加・利用し、業務での再利用が成立する (North Star) |
| G7 | Claude Code / Codex 契約のない社員にも Web App 出口でブラウザから成果が届く |

## U4 目標 (objectives)

| ID | 目標 | 測定基準 |
|---|---|---|
| O1 | 作者の自己完結 publish 成功率 80% (仮説 H1) | 提供者の代理作業なし・Git 操作ゼロで公開完了した一般作者の割合 ≥ 80% (Stage 1 開始後の初回計測。数値は Stage 0 実測で更新) |
| O2 | 提供者の代理作業比率 30% 以下 (仮説 H4) | 全 publish 件数のうち、提供者が代理実行または介入した件数の割合 ≤ 30% |
| O3 | 公開までのリードタイム短縮 (仮説 H5) | 数値基準は未設定。qa-267 の中立再確認で、利用者が『旧 O1-O4 へ完全に戻す』を選択した際に、O3 が完了判定できない目標として残る不利点を了解したうえで確定した。実装フェーズ前に基準値を定める場合は改めて承認を要する |
| O4 | 初回 publish 15 分以内 (仮説 H8) | 作者が初回 publish を開始してから公開完了までの所要時間 ≤ 15 分 |

## U5 成功基準 (success_criteria)

- 導入したハーネスによる月次の削減時間 (人・時) が、実行ログから算出した値として可視化され、その値が事業判断に使える水準に達していること。判定単位は月次の削減時間とし、金額換算は顧客ごとに時給単価の前提が異なり顧客間で比較できないため成功判定の正本にしない (提示資料での併記は妨げない)。判定に用いる具体的な閾値は本承認では定めておらず、O3 と同様に実装フェーズ前に定める場合は改めて承認を要する。 (appr-062 / qa-298)

## U6 ステークホルダー (stakeholders)

- 作者: 顧客企業の非エンジニア社員。JTBD: 自分の業務課題を AI と自分で解決し、解決物を同僚へ届けたい
- 利用者: 作者の同僚 (Claude Code / Codex 契約の有無を問わない)
- Workspace 管理者: 公開範囲・承認・監査・owner 再割当・初回 Cloudflare 接続の統制を担う
- 提供者 (本リポジトリ owner): 教育・有料助言・Platform 運用。個別納品と受託保守は負わない
- 顧客側の経営層・決裁者: Hub の導入と継続を判断する。Hub を直接は操作しないが、月次の削減時間 (U5) を判断材料として必要とするため、効果測定の出力はこの読み手に届く形にする。 (appr-062 / qa-299 で追加)

## U7 スコープ (scope)

- **対象 (in)**: Stage 0: Discovery Pilot と technical gate 検証 (H3 / H6 / H7: wrangler CLI 公開・URL 型 marketplace・Bootstrap Installer), Stage 1: Publisher + Thin Dual Catalog MVP (skills-only package の Green 自動公開・Project / TargetChannel / Release / CatalogEntry・version 自動採番・stable pointer と rollback・Workspace Catalog), マルチテナント論理分離を Stage 1 から実装する (1 つの Hub に複数顧客 Workspace を同居させ、データ・権限・Catalog を Workspace 境界で分離する。コンセプト文書の 1 Workspace 想定からの変更点), Web App 出口 (作者 local session での wrangler CLI スクリプト実行 + Hub への URL・release 登録), Studio 拡張: 効果測定・ダッシュボード (実行ログ ingest / 週次 rollup / KPI・部門別・ユーザー別集計。旧 Stage 4 対象外の Result Dashboard 相当を前倒しでスコープ入り。根拠: ユーザー指示 2026-07-17 = Harness Studio mockup 全反映)【appr-062 / qa-300 で範囲内と明示。U9 (qa-302) の最優先 2 件のうち 1 件であり、U5 の成功判定 (月次の削減時間) を機械的に算出する基盤そのものであるため、Stage 1 と同時に作る】, Studio 拡張: 改善要望・レビューのフィードバックループ (CLI + Web の 2 経路受付。AI 自動対応は D5 の pull 型キュー), Studio 拡張: ドキュメント CMS (common/tenant スコープ・AI 下書き・XSS sanitize 付き Markdown), Studio 拡張: ユーザー管理・アカウント設定 (role 管理は qa-005 の 4 role と統合。年収→時給換算の係数設定 = PII 保護対象。通知設定のメール送信は D6), 顧客業務データの保持 (C4 改訂 2026-07-18・appr-007): 業務ナレッジ/ドキュメントとハーネス実行の入出力データを R2 + テナント別封筒暗号化で保管し、即時完全削除ポリシーで管理する (qa-045/qa-046/qa-047/qa-048。qa-047/qa-048 は採番衝突により旧 qa-043/qa-044 から再登録)
- **対象外 (out)**: 独自 Web App runtime / Hosted Harness runtime (恒久非目標。§6.2), 共通 Runtime Connector (Stage 5 の条件付き拡張), 独自 Cloudflare deployment engine (Stage 3 の条件付き拡張), Hook・script・binary の自動公開 (Stage 5 の review 経路), 公開 marketplace・SNS・ランキング・収益分配 (Stage 5 以降), Web 会話型 Creator (非目標。Web 起点の作成需要が反復確認された場合のみ再検討), パスワードログイン・2FA の自前実装 (Studio mockup の login 画面は不採用。D3 の顧客 IdP / SSO 委譲を維持), visibility=public の公開範囲 (mockup の public は Stage 5 の公開 marketplace 相当。当面 private / workspace まで), `plugin install ./zip` 手動配布の正式経路化 (障害時の代替経路として記載するのみ), 顧客業務システムの接続 credential の保管 (C4 改訂でも保持対象外。ユーザー選択 2026-07-18), Stage 2: Workspace Governance (管理者 approval queue・granular RBAC・formal audit log・Yellow review・update 通知)【appr-062 / qa-300 により今回の範囲外。管理者 approval queue・granular RBAC・formal audit log・Yellow review・update 通知は Stage 1 稼働後に改めて範囲へ入れる。改善要望機能が前提とする member / workspace-admin / provider-admin の基本的な権限区分は qa-005 の 4 role に基づき Stage 1 内に残る (granular RBAC とは別物)。】, Studio 拡張: Web ヒアリング intake (4 ステップウィザード・削減効果の自動試算・受付番号採番) とヒアリングシート管理【appr-062 / qa-300 により今回の範囲外。】, Studio 拡張: 構築パイプライン可視化 (ヒアリング→要件定義→設計→構築→テスト→レビュー→公開の 7 工程ボード。工程操作は admin)【appr-062 / qa-300 により今回の範囲外。】

## U8 制約 (constraints)

- C1 開発体制: 実装・運用は提供者 1 名 + AI (Claude Code / Codex) のみ。運用負荷の低さと保守性を技術選定の最優先基準とする
- C2 インフラコスト: 固定費を極小化する (従量課金・無料枠優先)。顧客 Workspace 数が増えても固定費が比例して増えないこと
- C3 既存資産再利用: harness-creator / package contract / package check / marketplace catalog / version・cache 処理 / review workflow を Publisher 内部の quality engine として再利用する (§15)
- C4 データ保持境界 (2026-07-18 改訂・appr-007): Hub は顧客の業務ナレッジ/ドキュメントとハーネス実行の入出力データを保持できる (保存 = R2 + テナント別封筒暗号化 [purpose=tenant_data]・削除 = ユーザー操作/解約時の即時完全削除・アクセスは認可 MW 通過後のみ復号・監査 event 必須)。顧客業務システムの接続 credential と Web App runtime は引き続き保持しない (§6.2 / HH-D07 の runtime 境界は不変。旧 N2 の撤回と脅威 T14/T15 の追加は qa-046)

## U9 具体的にやりたいこと (concrete_intents)

| ID | やりたいこと | 資するゴール |
|---|---|---|
| I1 | 公開を 1 操作にする (/harness-hub:publish または自然言語)。branch / commit / PR / CI / merge / version / cache を利用者から隠す | G1 |
| I2 | static validation・secret scan・policy 判定 (Green 自動公開、Yellow / Red は Needs Fix 差し戻し) | G1, G4 |
| I3 | immutable Release + TargetChannel 別 stable pointer による atomic な公開・更新・rollback | G1, G4 |
| I4 | Workspace Catalog (業務ツール一覧・詳細・「追加する」「Web アプリを開く」導線・低品質報告導線) | G2, G3 |
| I5 | Web App 出口: 作者 local session で Publisher が wrangler CLI をスクリプト実行し、Hub は URL 登録・公開範囲検査・health 確認のみ担う【appr-062 / qa-302: U9 の最優先 2 件のうち 1 件】 | G3 |
| I6 | URL 型 marketplace (native source) または Bootstrap Installer による Git レス配布・更新 (一般利用者に GitHub アカウントを要求しない) | G1, G2 |
| I7 | マルチ Workspace 論理分離と顧客既存 IdP / SSO への認証委譲 (Hub 独自アカウント基盤を作らない) | G4 |
| I8 | Stage 2 Governance: 管理者 approval queue・granular RBAC・formal audit log・export【appr-062 / qa-300: 今回の範囲外へ移動。Stage 1 稼働後に再度範囲へ入れる】 | G4 |
| I9 | Stage 0 technical gate: URL 型 marketplace / Bootstrap Installer / wrangler 公開の成立検証 (H3 / H6 / H7) と Stage 1 開始条件の判定 | G1, G3 |
| I10 | 効果測定: 実行ログ ingest (短命 token・冪等キー・クライアントは回数のみ送信) + サーバ側係数換算 (時給 = 年収 ÷ annualHours・分/回・削減率はテナント設定) + Workers cron の週次 rollup で S09/S16/S17 のダッシュボードへ供給【appr-062 / qa-302: U9 の最優先 2 件のうち 1 件】 | G4, G5 |
| I11 | ヒアリング intake: S10 の 4 ステップウィザード → 受付番号採番 → D5 pull 型 AI キューでヒアリングシート生成 (S11/S12) → S13 の 7 工程パイプラインで構築進行を可視化【appr-062 / qa-300: このうち Web ヒアリング intake と構築パイプライン可視化は今回の範囲外】 | G1, G5 |
| I12 | フィードバックループ: CLI (claude harness feedback) + Web フォーム (S14) の 2 経路受付 → AI 対応 (D5) → 修正版 publish → update 通知 | G2, G5 |
| I13 | ドキュメント CMS (S15): common/tenant スコープの Markdown 文書 (共通レンダラで XSS sanitize)・AI 下書き (D5) | G2 |
| I14 | ユーザー管理・アカウント設定 (S17/S18): role 管理 (qa-005 の 4 role と統合)・年収係数 (PII: admin 限定・一般 API 非公開・export マスク)・通知設定 (メールは D6 = Resend Free) | G4, G5 |
| I15 | Hub の全認証済み業務画面の右下に常設した「改善要望」ボタンから、画面を離れずに DOM 再描画スクリーンショット・注釈・黒塗り付きの改善要望を投稿でき、投稿者には本文と注釈以外の入力を課さないまま、裏側で DevTools 相当の診断情報 (console error/warn、未捕捉例外、失敗した network request、viewport/DPR/theme、route pattern、build 版数、直近 navigation) を自動収集して同送し、その内容から不具合報告 (bug) か機能要望 (request) かをサーバ側で推定して初期値とし、provider-admin と workspace-admin が一元管理画面で種別・対応状態・メモを回し、投稿を Claude Code 向けの改善指示 (固定 frontmatter と固定 6 節の Markdown) へ組み立てて GitHub Issue として起票し、Claude Code 側が Issue を読んでそのまま実行することで改修へつなげる【appr-061: 出口を Hub 自身の pull 型 API から GitHub Issue へ戻した。利用者が既に GitHub の API キーを持っており運用が楽である、という理由による】。スクリーンショットと診断ファイルは GitHub Contents API で対象リポジトリの`improvement-requests/<要望 ID>/` へ commit し、その raw URL を Issue 本文へMarkdown 参照として書く【appr-063: GitHub は Issue への画像添付を公式 API として提供していないため、公式経路である Contents API による commit を利用者が選んだ】。指示文は bug では診断詳細と再現の手掛かりを展開し request ではサマリと上位のみに留め、指示文 1 本を読めばそのまま実装できる情報一式 (本文・注釈画像の所在・診断サマリと詳細・該当 route と想定変更箇所・受入条件雛形) を AI 改善へ引き渡す。情報量は各段で有界にし、収集は指紋による畳み込みと件数上限、診断は総量 32KB と削り順の固定、指示文は 60,000 文字予算と種別別の掲載件数で抑え、溢れた分は commit した診断ファイルと管理画面の詳細 URL で所在を示す。管理画面の一覧はテーブルとして選択と一括操作を持ち、起票は起票状態と本文ハッシュの一致から新規・更新・変更なしを導出して同じ要望に Issue を重複させず、詳細で編集された内容は同じ Issue を更新する。画像・診断ファイルの commit 先 path は要望 ID から決まる固定形なので、本文に raw URL を埋めても本文ハッシュは安定する。認証は Cloudflare Workers Secret に置く GitHub トークン 1 本に限り、必要権限は Issues: Read and write と Contents: Read and write を対象 1 リポジトリへ限定する。取得先と必要権限の案内は共有定数から 3 箇所へ同じ文言で出す。誤投稿や成立していない投稿は理由付きの廃棄として一覧から外して復帰でき、廃棄済みに限り provider-admin が画像と診断の実体を消す削除を行って ID と削除記録だけの抜け殻を残し、起票済みの Issue は本文を差し替えて close し、commit したファイルを削除する commit を積む。ただし git 履歴からは消えないため、削除の効果が及ばない範囲を削除確認画面と運用手順書に明記する【appr-063 で受け入れたリスク】 | G4, G5 |

## 意思決定支援 (decisions)

| ID | 論点 | 状態 | 選択肢 (費用・適合・注意点) | AI推奨 | ユーザー決定 | 資するゴール |
|---|---|---|---|---|---|---|
| D1 | Hub 本体 (Next.js + TypeScript) の実行環境・hosting をどれにするか | confirmed | cf-workers-opennext:Cloudflare Workers 一体型 (@opennextjs/cloudflare) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠 (10万req/日) 内は月額0円。超過時は Workers Paid $5/月〜の従量。構築は OpenNext adapter の学習コストのみ、撤退時は Node hosting へ移行可能'} / free=100,000 リクエスト/日、CPU 時間制限あり (無料枠) / fit=Web App 出口が顧客側 Cloudflare (HH-D07) のため生態系が統一され、提供者の運用知見が 1 系統に集約される / pros=デプロイ対象が 1 つ (C1 適合), R2 native binding, 固定費ゼロ, wrangler 運用が Web App 出口と共通 / cons=OpenNext adapter 経由のため Next.js 全機能の互換確認が必要, Workers ランタイム制約 (Node API 一部非互換) / risks=OpenNext/Next.js のバージョン追随が遅れる可能性 / lock-in=中 / ops=低 / evidence=https://developers.cloudflare.com/workers/platform/pricing/, https://opennext.js.org/cloudflare<br>pages-plus-worker:Cloudflare Pages (UI) + 専用 Worker API (Hono) 分離 / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '両方無料枠内で月額0円。ただし 2 系統のビルド・デプロイ・認証連携の構築保守工数が加算'} / free=Pages: ビルド 500 回/月、Workers: 10万 req/日 / fit=API の独立進化には有利だが、個人運用 (C1) では管理対象が増える / pros=API を独立してスケール・進化できる, 関心の分離が明確 / cons=デプロイ 2 系統, 認証・セッションの連携設計が増える / risks=個人開発での保守負荷増 / lock-in=中 / ops=中 / evidence=https://developers.cloudflare.com/pages/<br>vercel-hobby:Vercel Hobby / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '月額0円だが商用利用不可のため、顧客展開時は Pro ($20/月/席) が必須になり費用ゼロ制約と衝突'} / free=Hobby は非商用利用限定 / fit=Next.js 最適化は最強だが、商用マルチテナント SaaS は Hobby 規約違反 / pros=Next.js 互換性が完全, DX が高い / cons=商用利用不可 (Hobby), R2/Turso へ native binding 不可 / risks=規約違反による停止リスク / lock-in=中 / ops=低 / evidence=https://vercel.com/docs/plans/hobby | cf-workers-opennext — 費用ゼロ制約 (C2 強化) と個人運用制約 (C1) の下で、Web App 出口 (顧客側 Cloudflare) と運用知見を 1 系統に統一でき、デプロイ対象が 1 つで済む (注意: OpenNext adapter の Next.js バージョン互換を実装着手時に公式ドキュメントで再確認する, Workers ランタイムの Node API 非互換 (一部ライブラリ) に注意, Cloudflare Workers Free プランの Worker サイズ上限は 3 MiB (gzip 後。Paid は 10 MiB)。CI で bundle サイズ計測を必須とし、超過時はコード分割・依存削減で対処する。恒常的に超過する場合は Workers Paid ($5/月) への移行と費用ゼロ制約 C2 の再交渉をユーザーへ差し戻す (fetched-references.json opennext-cloudflare の記録); confidence=high; checked=2026-07-16T00:00:00+09:00) | cf-workers-opennext @ 2026-07-16T00:00:00+09:00 | G1, G4 |
| D2 | Hub control-plane DB (Tenant/Workspace/Project/Release/Catalog/監査event) の永続化をどれにするか | confirmed | d1-drizzle:Cloudflare D1 + Drizzle ORM / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠 (5GB・読取500万行/日) 内は月額0円。Workers native binding で外部依存なし'} / free=5GB ストレージ、読取 500万行/日、書込 10万行/日 / fit=顧客 Web App の標準 DB (HH-D14) と同一技術で知見が共通化 / pros=native binding でレイテンシ最小, 外部サービス依存ゼロ, Drizzle で型安全 / cons=SQLite 由来の同時書込制約, リージョン単一 (read replica はベータ) / risks=大規模化時のスループット上限 / lock-in=中 / ops=低 / evidence=https://developers.cloudflare.com/d1/platform/pricing/, https://orm.drizzle.team/docs/connect-cloudflare-d1<br>turso-free:Turso Free (libSQL) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠内は月額0円。Workers からは @libsql/client (HTTP) 接続。無料枠改定時は D1 へ移行 (Drizzle 両対応で移行コスト小)'} / free=無料プランに DB 数・ストレージ・行読取の上限あり (公式 pricing 参照) / fit=SQLite 方言で D1 と互換。ブランチ機能・組込みレプリカなど開発体験が良い / pros=SQLite 互換で D1 退避経路を温存, DB ブランチなど開発機能, エッジレプリカ / cons=native binding ではなく HTTP 経由, 外部サービス依存が 1 つ増える / risks=スタートアップ依存の無料枠改定・事業継続リスク / lock-in=低 / ops=低 / evidence=https://docs.turso.tech/, https://turso.tech/pricing<br>supabase-free:Supabase Free (Postgres) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠は月額0円だが 1 週間非アクセスで pause。商用継続なら Pro $25/月が現実的で費用ゼロ制約と衝突'} / free=500MB DB、1 週間非アクティブで自動 pause、2 プロジェクトまで / fit=リレーショナル機能は最も充実するが、Workers からは HTTP 経由 / pros=Postgres の表現力, 管理 UI / cons=無料枠の pause 挙動が本番不適, SQLite 系と非互換で D1 との知見分断 / risks=無料枠での本番運用は実質不可 / lock-in=中 / ops=中 / evidence=https://supabase.com/pricing | d1-drizzle — native binding・外部依存ゼロ・顧客 Web App 標準 DB (HH-D14) との知見統一の 3 点で、個人運用と費用ゼロ制約に最も適合 (注意: D1 の同時書込特性と read replica の成熟度を実装着手時に再確認する; confidence=medium; checked=2026-07-16T00:00:00+09:00) | turso-free @ 2026-07-16T00:00:00+09:00 | G1, G2, G4 |
| D3 | Hub Web / API の認証・認可基盤をどれにするか (Hub 独自アカウント基盤は作らない前提) | confirmed | authjs-oidc:Auth.js (旧 NextAuth) + テナント別 OIDC / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'OSS で月額0円。構築はテナント別 OIDC 設定の動的解決実装、運用は IdP 設定変更対応のみ'} / free=制限なし (OSS) / fit=テナントごとに顧客既存 IdP (Google Workspace / Microsoft Entra ID) を登録でき、Hub 独自アカウント基盤を作らない §10.2 と整合 / pros=完全無料, Next.js との統合が自然, テナント別 IdP を Hub のデータモデルで管理できる / cons=OIDC 設定の動的マルチテナント解決は自前実装, セッション失効管理を自分で設計 / risks=実装ミスによる認可漏れ (分離テストで担保) / lock-in=低 / ops=中 / evidence=https://authjs.dev/, https://authjs.dev/getting-started/providers/microsoft-entra-id<br>cloudflare-access:Cloudflare Access (Zero Trust) 前段 / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '50 ユーザーまで月額0円。超過で有料プラン。アプリ側は JWT 検証のみで構築工数最小'} / free=無料は 50 ユーザーまで / fit=コード最小だが、顧客 IdP を提供者の Cloudflare アカウントへ登録する形になり Tenant 境界が Hub のデータモデル外に漏れる / pros=実装コード最小, IdP 連携が設定だけで済む / cons=50 ユーザー上限が複数顧客展開 (U5) と早期に衝突, テナント境界の管理が Cloudflare ダッシュボード依存 / risks=ユーザー数成長で即有償化 / lock-in=高 / ops=低 / evidence=https://developers.cloudflare.com/cloudflare-one/policies/access/, https://www.cloudflare.com/plans/zero-trust-services/<br>idaas-free:IDaaS (Clerk / Auth0 無料枠) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠は月額0円だが MAU 上限・機能制限あり。マルチテナント SSO (SAML/OIDC per tenant) は多くの IDaaS で有償上位プラン'} / free=MAU 上限・テナント別エンタープライズ SSO は有償プランが多い / fit=テナント別 IdP 連携がまさに有償機能に該当しやすく、費用ゼロ制約と衝突 / pros=実装最速, UI 込み / cons=テナント別 SSO が有償になりがち, 価格改定・ロックイン / risks=無料枠縮小の前例が業界に多い / lock-in=高 / ops=低 / evidence=https://clerk.com/pricing, https://auth0.com/pricing | authjs-oidc — 費用ゼロ・複数顧客同時展開 (U5)・テナント境界を Hub のデータモデルで管理する要件を同時に満たすのは Auth.js のみ (注意: マルチテナント OIDC の動的 provider 解決は Auth.js の標準外パターンのため設計レビューを行う, セッション失効・role 変更の反映遅延 (JWT) を許容時間として仕様化する, Auth.js は Better Auth 傘下入りが公式告知済み (fetched-references.json authjs の記録)。実装着手前に Better Auth の移行ガイド成熟度を再確認し、Auth.js のセキュリティ修正停止を Better Auth への移行トリガーとして明記する; confidence=high; checked=2026-07-16T00:00:00+09:00) | authjs-oidc @ 2026-07-16T00:00:00+09:00 | G2, G4 |
| D4 | マルチテナント論理分離 (U7) のテナント分離方式をどれにするか | confirmed | row-level-scope:単一 DB + tenant_id/workspace_id スコープ列 + アプリ層強制 (row-level 分離) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠の 1 DB 内で月額0円。構築はスコープ列とアプリ層ガードの実装、運用は分離テストの維持のみ'} / free=Turso Free の 1 DB 内に全テナントを収容 (ストレージ 5GB を共有) / fit=実装が単純で個人運用 (C1) に適合。qa-006 の『全 API で Tenant/Workspace スコープ強制 + 分離テスト必須』と同一方式で U7 の論理分離を満たす / pros=実装が単純でスキーマ・migration が 1 系統, 無料枠 1 DB で収まり費用ゼロ制約 (C2) に適合, D1 退避経路 (D2 ヘッジ) と対称で、単一 DB のまま移行できる / cons=分離保証が構造的でなくアプリ層実装とテストに依存する / risks=アプリ層ガードの実装漏れによるテナント間データ露出 (分離テストで検出する) / lock-in=低 / ops=低 / evidence=https://docs.turso.tech/, https://developers.cloudflare.com/d1/platform/pricing/<br>db-per-tenant:Turso DB-per-tenant (テナントごとに DB を分離) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'Turso Free は DB 100 個まで月額0円。ただし migration 一斉適用・接続管理の構築保守工数が加算'} / free=Turso Free はデータベース 100 個・ストレージ計 5GB (fetched-references.json turso の記録) / fit=構造的分離の保証は最も強いが、個人運用 (C1) では migration の一斉適用と接続管理の複雑さが負担になる / pros=テナント分離を DB 境界で構造的に保証できる, テナント単位のバックアップ・削除・退去が容易 / cons=migration の一斉適用と接続管理 (テナント→DB 解決) が複雑, D1 退避時に非対称 (単一 DB 前提の D1 退避経路と整合しない) / risks=テナント数増加時の migration 運用事故, D2 ヘッジ (D1 への退避) の移行コストが増大する / lock-in=中 / ops=中 / evidence=https://docs.turso.tech/, https://turso.tech/pricing<br>schema-per-tenant:schema 分離 (テナント別 schema/namespace) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加費用はないが、SQLite 系 (Turso/D1) には Postgres 的な schema 分離機構がなく実現手段を欠く'} / free=該当なし (SQLite 方言では schema 分離を実現できない) / fit=Postgres 系の定石だが、採用済みの Turso/D1 (SQLite 方言) には schema 概念がなく U7 の実現方式として非現実的 / pros=(Postgres 系であれば) DB 数を増やさず論理分離を構造化できる / cons=SQLite に native な schema 分離がない (ATTACH は Turso/D1 で非現実的), 実現には DB を Postgres 系へ変更する必要があり D2 決定と衝突する / risks=D2 (Turso/SQLite 方言) の決定と両立せず、採用すると技術選定の再交渉が発生する / lock-in=高 / ops=高 / evidence=https://www.sqlite.org/lang_attach.html | row-level-scope — 個人運用 (C1)・費用ゼロ (C2) の下で実装・運用が最も単純で、qa-006 で承認済みの『全 API で Tenant/Workspace スコープ強制 + 分離テスト必須』と整合し、D1 退避経路 (D2 ヘッジ) とも対称のまま U7 の論理分離を満たす (注意: 分離保証はアプリ層とテストに依存するため、Tenant/Workspace スコープの分離テストを CI 必須とする (qa-006), revisit 条件: テナント数が 10 を超える、または分離テストの失敗が頻発した場合は Turso DB-per-tenant を再評価する; confidence=high; checked=2026-07-16T00:00:00+09:00) | row-level-scope @ 2026-07-16T00:00:00+09:00 | G4 |
| D5 | AI 処理 (ヒアリングシート生成・フィードバック自動対応・ドキュメント下書き) の実行主体をどれにするか | confirmed | pull-queue:pull 型キュー (Claude Code セッションがキューを pull して処理・書戻し) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加サービスなしで月額0円。キューは既存 Turso のテーブル + Hub REST のみで構成し、AI 実行は提供者/作者の既存 Claude Code 契約内で行う'} / free=追加の外部サービスを持たない (Hub の既存 Workers / Turso 無料枠内のジョブテーブルと pull/書戻し API のみ) / fit=C2 (費用ゼロ) と完全整合し、mockup の「Claude Code が対応」という記述とも一致。シート生成 (I11)・FB 対応 (I12)・doc 下書き (I13) を同一キューで扱える / pros=サーバ側 AI 課金が発生せず固定費ゼロを維持できる, AI 品質は利用者側の Claude Code (Claude 本体) に等しく、生成品質の妥協がない, キューが疎結合のため、将来 server-side 実行へ差し替えても API 契約を変えずに移行できる / cons=処理はセッション稼働時のみ (完全非同期・数分〜の遅延を UI が許容する設計になる), pull ワーカーの稼働は提供者/作者の運用習慣に依存する / risks=キュー滞留 (誰も pull しない期間) — 滞留監視と滞留アラートを保守運用に組み込んで検出する (qa-027) / lock-in=低 / ops=低 / evidence=https://docs.claude.com/en/docs/claude-code/overview, https://developers.cloudflare.com/workers/platform/pricing/<br>server-anthropic-api:サーバ側で Anthropic API を直接呼ぶ (Workers から従量課金) / cost={'category': 'paid', 'amount': 1500, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'トークン従量課金で利用量に比例 (月間ジョブ数×平均トークン量に依存し固定上限がない)。概算最低でも月 1,500 円規模から利用増で線形に増加'} / free=恒常的な無料枠なし (トークン従量課金) / fit=即時処理 (同期 UX) を実現できるが、利用が伸びるほど費用が増える構造が C2 (固定費極小化・無料枠優先) と衝突する / pros=ジョブを即時処理でき、生成中の待ち時間が最短になる, 処理が提供者のセッション稼働に依存しない / cons=従量課金が C2 と衝突する, Hub に AI 資格情報を保持する必要がある / risks=利用増による費用の青天井化 (テナント数×ジョブ数に比例) / lock-in=中 / ops=中 / evidence=https://www.anthropic.com/pricing<br>workers-ai:Cloudflare Workers AI (無料枠 10,000 neurons/日) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠 10,000 neurons/日以内なら月額0円 (超過は Free プランではハードリミットで失敗、00:00 UTC リセット)。公式確認 2026-07-17'} / free=10,000 neurons/日 (Free プランは超過時ハードリミット)。neurons から unit-based 価格への改定が告知されており枠の再確認が必要 / fit=費用ゼロは満たすが、Claude ではなく OSS モデルのため日本語の長文ヒアリングシート生成・コード修正案の品質が要件水準に届かないリスクがある / pros=費用ゼロで即時処理できる, 追加ベンダーが増えない (Cloudflare 内で完結) / cons=OSS モデルの生成品質が Claude に劣り、シート生成・修正案の品質保証が難しい, ハードリミット超過時は当日中の処理が失敗する / risks=価格モデル改定 (neurons → unit-based) が告知済みで、無料枠の条件が変わり得る / lock-in=中 / ops=中 / evidence=https://developers.cloudflare.com/workers-ai/platform/pricing/ | pull-queue — C2 (費用ゼロ) を満たす 2 案のうち、生成品質を Claude 本体に委ねられ、mockup の「Claude Code が対応」という動線とも一致する pull 型が唯一品質と費用を両立する。非同期遅延は UI の「生成中」ステータス (mockup 準拠) で吸収できる (注意: 処理遅延は数分〜セッション次第。UI は非同期前提 (受付番号 + 生成中ステータス + 完了通知) で設計する, キュー滞留の監視・アラートを保守運用 (qa-027) に必ず組み込む, revisit 条件: 即時性への要望が反復確認された場合、または提供者の pull 運用が回らない場合は Workers AI 併用 (下書きのみ) を再評価する; confidence=high; checked=2026-07-17T17:52:19+09:00) | pull-queue @ 2026-07-17T17:52:19+09:00 | G1, G5 |
| D6 | メール通知 (生成完了・レビュー結果・週次サマリ) の送信手段をどれにするか | confirmed | resend-free:Resend Free プラン (トランザクションメール API) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '無料枠 3,000 通/月・100 通/日・独自ドメイン 1 件の範囲で月額0円。想定規模 (数 Workspace × 週次サマリ + イベント通知) は枠内に収まる。公式確認 2026-07-17'} / free=3,000 通/月・100 通/日・独自ドメイン 1 件 (送受信合算。To/CC/BCC の複数宛先は宛先数分カウント。無料プランは超過時に送信不可でオーバーエイジなし) / fit=Workers から直接 REST API を呼べて実装・運用が単純 (C1 適合)。update 通知 (G2) と週次サマリ (G5) を無料枠内で賄える / pros=無料枠が要件規模に対して十分 (3,000 通/月), SDK / REST が単純で Workers から直接送信できる, ドメイン認証 (SPF/DKIM) 手順が整備されており到達率を確保しやすい / cons=日次 100 通の上限があり一斉通知はバッチ分割が必要, 独自ドメイン 1 件までのため送信ドメインは Hub 共通になる / risks=無料枠の条件変更 — 送信量が枠に近づいたら通知の要約化 (ダイジェスト) で吸収する / lock-in=低 / ops=低 / evidence=https://resend.com/pricing, https://resend.com/docs/knowledge-base/account-quotas-and-limits<br>in-app-only:アプリ内通知のみで開始 (メール送信なし) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '外部送信サービスを持たず月額0円。通知センターは既存 Workers/Turso 無料枠内で完結'} / free=追加サービスなし (Hub 内の Notification テーブル + UI のみ) / fit=費用・運用ゼロで最も C1/C2 に忠実だが、Hub を開いていない利用者に更新 (G2) や週次サマリ (G5) が届かず、mockup のメール通知トグルが空機能になる / pros=費用・運用ともゼロ, 到達率・ドメイン認証の考慮が不要 / cons=Hub 非訪問ユーザーへ通知が届かない (update 通知の実効性が下がる), mockup のメール通知設定と齟齬が出る / risks=更新に気づかれず再利用率 (O2) の計測が鈍る可能性 / lock-in=低 / ops=低 / evidence=https://developers.cloudflare.com/workers/platform/pricing/<br>amazon-ses:Amazon SES (従量課金) / cost={'category': 'low-cost', 'amount': 15, 'currency': 'JPY', 'billing_period': 'month', 'tco': '$0.10/1,000 通の従量で月 1,000 通なら約 15 円。ただし AWS アカウント・IAM・サンドボックス解除・評判管理の運用が加算される'} / free=恒常無料枠は限定的 (送信条件依存)。従量 $0.10/1,000 通 / fit=単価は最安級だが、Cloudflare 中心の構成に AWS 運用 (IAM・サンドボックス解除・バウンス管理) が加わり C1 (個人運用の認知負荷) と不整合 / pros=送信単価が最安級, 大規模化しても料金が線形 / cons=AWS アカウント/IAM/サンドボックス解除の初期・運用負荷, ベンダーが 1 社増える / risks=サンドボックス解除審査や評判低下による送信停止 / lock-in=中 / ops=中 / evidence=https://aws.amazon.com/ses/pricing/ | resend-free — 想定規模が無料枠 (3,000 通/月・100 通/日) に収まり、Workers から直接呼べて個人運用 (C1) の負荷が最小。アプリ内通知のみでは G2 (更新に気づく) の実効性が下がり、SES は運用負荷が C1 と不整合 (注意: 送信は必ず通知ディスパッチ共通層を経由し、Resend API を feature 実装から直接呼ばない (差し替え可能性の維持), 日次 100 通制限があるため週次サマリはバッチ分割 + 失敗リトライで送る (qa-027), アプリ内通知を常に正本とし、メールは補助チャネル (不達でも情報が欠けない設計); confidence=high; checked=2026-07-17T17:52:19+09:00) | resend-free @ 2026-07-17T17:52:19+09:00 | G2, G5 |
| D7 | 環境構成: 常設 staging 環境を持つか、preview 使い捨てのみとするか (qa-034 と qa-038 の矛盾解消) | confirmed | persistent-staging:production + 常設 staging の 2 環境 (qa-034 の当初確定) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'Turso Free 100 DB / Workers Free の枠内で金銭コストは 0 円。ただし Worker・DB・R2 prefix・secret を 2 組維持するため無料枠の消費とローテーション対象が 2 倍になる'} / free=Workers 10万 req/日・Turso 5GB/読取5億行/月 を 2 環境で分け合う / fit=migration 検証と restore drill の受け皿が常設される / pros=migration を本番前に常設環境で検証できる, restore drill の投入先が常に存在する / cons=無料枠を二重消費する, secret / binding / ドメインの管理対象が 2 倍になり C1 (提供者1名運用) の負荷が増える, staging 固有の drift が生じ本番と乖離しうる / risks=無料枠の逼迫が production 側の可用性に波及する / lock-in=低 / ops=高 (2 組の secret・migration・監視を維持) / evidence=https://developers.cloudflare.com/workers/platform/limits/, https://turso.tech/pricing<br>ephemeral-preview-only:常設 staging を持たず preview は PR ごとに使い捨て (qa-038【3】) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '常設リソースは production 1 組のみ。preview は PR close で破棄されるため無料枠の常時消費が増えない'} / free=Workers 10万 req/日・Turso 5GB を production が専有 / fit=C2 (固定費極小) と C1 (個人運用) を最優先しつつ、PR 単位の動作確認は preview で担保 / pros=無料枠と運用導線が二重化しない, 環境間 drift が生じない, secret 管理対象が最小 / cons=常設の migration 検証先が無く、CI の破壊的 DDL 検査に依存する, restore drill は都度一時 DB を作成する手間がある / risks=migration 事故が production へ直接及ぶ — expand/contract 3 段階強制と CI の DDL 検査で緩和する / lock-in=低 / ops=低 (production 1 組のみ) / evidence=https://developers.cloudflare.com/workers/configuration/versions-and-deployments/, https://developers.cloudflare.com/workers/platform/limits/ | ephemeral-preview-only — qa-038 は qa-034 より後に確定しており、常設 staging を否定する理由 (Worker/DB/R2/secret を 2 組維持すると無料枠消費と運用導線が二重化し C1・C2 と衝突する) を明示している。feat-hub-foundation P03 の独立設計レビュー (指摘 R-01) が両者の矛盾を検出し、上位判断としてユーザーへエスカレーションした。 (注意: 常設の migration 検証先が無くなるため、CI の破壊的 DDL 検査 (expand/contract 3 段階強制) が事実上の唯一の防波堤になる。この検査を無効化・スキップしてはならない, restore drill は一時 DB を都度作成する運用になるため、手順が runbook に明文化されていないと四半期ごとの実施が形骸化しやすい, preview 環境の払い出しと破棄が自動化されない場合、リソースが残存して無料枠を消費する。PR close での破棄を確認する導線が必要, qa-034 を参照している既存記述 (docs/infrastructure-spec.md 等) が改訂前提のまま残ると二重正本になる。本決定の反映範囲を追跡すること; confidence=high; checked=2026-07-21T08:00:00+09:00) | ephemeral-preview-only @ 2026-07-21T08:00:00+09:00 | G4, G5 |
| D8 | UI コンポーネントテストのツール選定: UI 微調整 (ボタン配置変更等) で壊れず保守しやすい behavior ベーステスト (qa-078 の保守性要件) を何で実装するか | confirmed | testing-library-react:@testing-library/react + Vitest (behavior ベースのコンポーネントテスト) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'OSS (MIT)。金銭コスト 0 円。Vitest 環境に @testing-library/react + @testing-library/dom (peer dependency) を追加するのみで、CI 実行時間への影響も component 単位テストのため軽微'} / free=なし (OSS) / fit=guiding principle『ソフトウェアの使われ方に近いテストほど信頼できる』が qa-078 の behavior ベース要件 (accessible role/ラベルでの要素選択・実装詳細への密結合禁止) と直接一致し、UI 微調整で壊れないテストを標準化できる / pros=ボタン配置・DOM 構造の微修正でテストが壊れない (要素選択が role/ラベル基準), コンポーネント単位で高速に実行でき、カバレッジ 80% ゲート (qa-077) の主担体になれる, React 公式ドキュメントも推奨する事実上の標準で、知見・情報が豊富 / cons=実ブラウザではなく jsdom/happy-dom 環境のため、レイアウト起因の不具合は検出できない (E2E の Playwright と役割分担が必要) / risks=v16 以降 @testing-library/dom の peer dependency 明示が必要 (導入手順に明記して回避) / lock-in=低 (テストコードは Testing Library API に依存するが、behavior ベースの書き方自体は他ツールへ移植しやすい) / ops=低 (追加設定は Vitest 環境への組込のみ) / evidence=https://testing-library.com/docs/react-testing-library/intro/, https://vitest.dev/blog/vitest-4-1.html<br>playwright-e2e-only:コンポーネントテストを持たず Playwright E2E のみで UI を検証 / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'OSS (Apache-2.0)。金銭コスト 0 円。ただし E2E はブラウザ起動を伴い CI 実行時間が component テストより大きい'} / free=なし (OSS) / fit=テストツールが 1 つ減り管理は軽いが、UI 不具合の検出が E2E 層まで遅延し、qa-076 のテストレベル網羅 (単体・結合) に UI コンポーネント層の空白が生じる / pros=ツール数が最小で管理が軽い, 実ブラウザでの検証のためレイアウト起因の不具合も検出できる / cons=コンポーネント単位の高速なフィードバックループがない, E2E は実行が重く、カバレッジ 80% ゲートの主担体にすると CI 時間が肥大する, 失敗時の原因切り分けが component テストより粗い / risks=UI 層の回帰検出が遅れ、qa-077 の改善ループ (失敗分析→修正→再実行) の回転が遅くなる / lock-in=低 / ops=中 (E2E の flake 管理が UI 検証全体を背負う) / evidence=https://playwright.dev/docs/release-notes | testing-library-react — qa-078 の確定要件 (behavior ベース・実装詳細への密結合禁止・UI 微修正でテストエラーにならない管理しやすい設計) を最も直接に実現する標準ツールであり、Vitest (単体・結合)・Playwright (E2E) との 3 点構成でテストレベル網羅 (qa-076) の各層に主担体を配置できるため。 (注意: jsdom/happy-dom 環境ではレイアウト起因の不具合を検出できないため、実ブラウザ検証は Playwright E2E と役割分担する (component テストの守備範囲を越えた検証を書かない), @testing-library/react v16 以降は @testing-library/dom を peer dependency として明示インストールする必要がある, role/ラベルで特定できない要素に data-testid を乱用すると behavior ベースの利点が失われるため、accessible role を優先する選択規律を維持する; confidence=high; checked=2026-07-24T12:11:57Z) | testing-library-react @ 2026-07-24T21:11:57+09:00 | G1, G5 |
| D9 | 改善要望ウィジェットの画面キャプチャ方式: 認証済み業務画面を、投稿者に追加の許可操作を求めずに 1 枚の画像として取得する手段を何にするか | confirmed | modern-screenshot:modern-screenshot 4.7.0 の domToCanvas による DOM 再描画 (SVG foreignObject 経路) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'OSS (MIT)。金銭コスト 0 円。クライアント bundle へ載る分の転送量が増えるが、改善要望ウィジェットを遅延読込にすることで初期表示には影響しない'} / free=なし (OSS) / fit=「押した次の瞬間に書き込める」という体験 (I15) を満たす唯一の経路。許可ダイアログを挟まず、押した時点の DOM をそのまま画像化できる / pros=許可ダイアログが出ないため、押してから注釈までの流れが途切れない, 撮影対象を DOM 単位で除外・変形でき、sticky ヘッダの位置合わせや内部スクロール箱の展開ができる, html-to-image / html2canvas より新しく、CSS の対応範囲が広い / cons=DOM を SVG foreignObject で再描画するため、実際の画面と完全に同一ではない (canvas 要素・cross-origin 画像・一部の CSS は再現されない), クライアント bundle が増える / risks=cross-origin 画像を含む画面で描画が欠ける。改善要望の目的 (どこが問題か伝える) には致命的でないが、欠けたことが分かる形にする必要がある / lock-in=低 (domToCanvas 相当の API は html-to-image も持ち、呼び出し 1 箇所を差し替えれば移行できる) / ops=低 (クライアント依存のみ。サーバ側の運用は増えない) / evidence=https://www.npmjs.com/package/modern-screenshot<br>get-display-media:ブラウザ標準の Screen Capture API (navigator.mediaDevices.getDisplayMedia) で実画面を取得 / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'ブラウザ標準機能のため依存追加なし。金銭コスト 0 円、bundle 増加も 0'} / free=なし (ブラウザ標準) / fit=実際に見えている画素をそのまま取得できるため再現性は最も高いが、撮影のたびに共有対象の選択と許可を求められ、I15 の「画面を離れずに投稿する」体験が壊れる / pros=実画面そのままで、CSS や canvas の再現性の問題が起きない, 依存パッケージが不要 / cons=撮影のたびに許可ダイアログと共有対象の選択が要る, 投稿者が誤って別ウィンドウを選ぶと、業務と無関係な画面が管理者へ送られる, モバイルブラウザでの対応が限定的 / risks=許可の手数が増えることで改善要望自体が出されなくなる (G5 の改善ループが回らない) / lock-in=なし / ops=低 / evidence=https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getDisplayMedia<br>html-to-image:html-to-image 1.11.13 (同系統の先行実装) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'OSS (MIT)。金銭コスト 0 円'} / free=なし (OSS) / fit=modern-screenshot と同じ DOM 再描画方式で体験は同等 / pros=実績が長く、利用例が多い / cons=最終更新が 2025-04 で、modern-screenshot より新しい CSS への追随が遅い, clone 時のフック (onCloneNode) など、sticky ヘッダの位置合わせに使う制御が弱い / risks=更新が停滞した場合、新しいブラウザ挙動への追随を自前で回避する必要がある / lock-in=低 / ops=低 / evidence=https://www.npmjs.com/package/html-to-image | modern-screenshot — I15 が求める体験の核心は「押した次の瞬間に書き込める」ことであり、許可ダイアログを挟む getDisplayMedia はこの一点で不適格となる。DOM 再描画方式の 2 案のうち、撮影対象の除外と clone 時の変形 (sticky ヘッダの位置合わせ・内部スクロール箱の展開) を制御でき、更新も新しい modern-screenshot を採る。再現性の限界 (canvas・cross-origin 画像) は、注釈と本文で補える範囲にとどまる。 (注意: DOM 再描画方式のため、canvas 要素・cross-origin 画像・一部の CSS フィルタは画像へ再現されない。撮影結果に欠けがあることを投稿画面で分かる形にし、本文と注釈で補えるようにする, widget 自身 (.feedback-root) の filter 除外と、sticky ヘッダの onCloneNode 位置合わせは自前実装であり、レイアウト変更時に追随が要る, 撮影処理はメインスレッドを占有する。大きな画面での所要時間を計測し、しきい値を超える場合は画質を落とさない範囲で対象を絞る; confidence=high — 3 案の差は「許可ダイアログの有無」という体験上の一点で決まり、判定に曖昧さがない。残る不確実性は再現性の限界の程度で、採否そのものは動かない; checked=2026-08-15T01:24:46Z) | modern-screenshot @ 2026-08-15T01:24:46Z | G1, G2, G5 |
| D10 | 改善要望を AI の改修作業へ渡す出口をどう実装するか (経緯: 旧 D10 は『GitHub Issue の起票・更新・検索を Workers から行う手段』だったが、appr-048 で GitHub Issue を出口としない方針が確定し同 id を新しい問いへ置き換えた。さらに appr-061 で利用者が appr-048 以前へ完全に戻す判断をしたため、本問いは維持したまま選択肢へ github-issue を加えて再確定する) | confirmed | github-issue:改善要望を GitHub Issue として起票し、Claude Code は GitHub 側から Issue を読んで実行する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'Workers から GitHub REST API を叩く実装が要る。Issue の起票・更新に加え、appr-063 により画像と診断ファイルを Contents API で commit する経路も乗る。外部書込みの失敗処理・再送・冪等の設計が実装に含まれる'} / free=GitHub API の認証済みレート制限 (5,000 requests/hour)。改善要望の投稿は 1 日あたり数十件の規模で、枠に対して余裕がある / fit=利用者が既に GitHub アカウントと token を持っており、Issue の一覧・検索・コメント・close といった対応の道具立てを新規に作らずに済む。AI へ渡す出口も、対応する人が見る場所も GitHub の 1 箇所に揃う / pros=対応する人は Issue だけを見れば状況を再現でき、Hub へログインし直さずに済む, Issue の検索・ラベル・コメント・close をそのまま対応管理に使える (Hub 側に作り込まなくてよい), Claude Code 側は既存の GitHub 連携で読めるため、専用 plugin を用意しなくても着手できる / cons=Hub から外部への書込みが発生するため、孤児 (外部に作ったが手元に記録できない) の失敗様式を設計で潰す必要がある, commit した画像と診断は git 履歴に残り、要望を削除しても履歴からは消せない (appr-063), トークンの権限が Issue 起票だけより強くなる (contents: write) / risks=トークン漏洩の影響が Issue の改変にとどまらず、対象リポジトリの改変に及ぶ。対象を 1 リポジトリに限定し Workers Secret から出さないことで抑える, public リポジトリを選んだ場合、未黒塗りのスクリーンショットが誰でも閲覧できる。設定画面に事実として表示することで抑える (塞ぐことは利用者の決定により行わない) / lock-in=中 (GitHub の Issues API と Contents API に依存する) / ops=中 (外部書込みの失敗再送とトークン更新を運用に持つ) / evidence=https://docs.github.com/en/rest/issues/issues, https://docs.github.com/en/rest/repos/contents, https://docs.github.com/en/get-started/writing-on-github/working-with-advanced-formatting/attaching-files<br>hub-pull-api:Hub が改善指示 API (read + ack) を公開し、Claude Code 側の plugin が取得して実行する pull 型 / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '既存 Worker (harness-hub) に route を 3 本足すだけ。外部サービスの追加契約なし。実装コストは指示文組み立て・token 検証・ack の状態遷移に収まる'} / free=Cloudflare Workers の Free プラン (100,000 requests/日) の内数。1 日あたりの改善指示取得は数十回の規模で、枠に対して無視できる / fit=改善要望の正本は Hub の DB であり、指示文はそこからの導出物である。pull 型なら Hub は外部へ書き込まず、配信状態も自分の DB だけで完結する。G5 の改善ループ (フィードバック→AI 対応→再公開) を、外部サービスを挟まずに閉じられる / pros=Hub の外側に副作用が無いため、POST 成功後に DB 更新が落ちて外部に孤児が残る、という失敗様式そのものが消える, 指示文の内容が変わったときは同じ行の版を上げるだけでよく、外部 ID の突き合わせが要らない, Claude Code 側は 1 つの slash command で『取ってきて実行して報告する』が完結する / cons=Claude Code 側に plugin (slash command + skill) を用意する必要がある, Hub が停止していると指示を取得できない (ただし改善要望の投稿自体も同時に止まるため、可用性の前提は変わらない) / risks=agent token が漏れると当該 workspace の改善要望本文と診断情報が読み取られる。scope を読み取りと ack に限り、有効期限と失効を管理画面から行えるようにして抑える / lock-in=なし (Hub 自身の API であり、外部サービスへの依存を持たない) / ops=低 (依存の追随なし。監視は既存の Worker 監視へ相乗り) / evidence=https://code.claude.com/docs/en/plugin-marketplaces, https://developers.cloudflare.com/workers/platform/pricing/<br>repo-file-export:Hub が指示文を repository へファイルとして書き出し、Claude Code はそのファイルを読む / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'GitHub Contents API の呼び出しを Workers から行う実装が要る。金銭コストは 0 円だが、外部書込みの失敗処理・再送・競合解決が実装に乗る'} / free=GitHub API の認証済みレート制限 (5,000 requests/hour) / fit=Claude Code は手元のファイルを読むだけでよく、取得側の実装は最も軽い。一方で Hub から外部への書込みが復活するため、Issue を廃止した理由がそのまま残る / pros=Claude Code 側に plugin を用意しなくても、ファイルを読ませるだけで動く, 指示文が git 履歴に残り、いつ何を指示したかを後から追える / cons=Hub から外部への書込みが必要で、孤児・二重作成・再送の設計が再び要る, 内容が変わるたびに commit が積まれ、repository が指示文で膨らむ, 廃棄・削除した要望の指示文を git 履歴から消せない / risks=書込み権限を持つ token の漏洩は、改善要望の閲覧ではなく repository の改変につながる / lock-in=中 (GitHub の Contents API に依存する) / ops=中 (外部書込みの失敗再送を運用に持つ) / evidence=https://docs.github.com/en/rest/repos/contents | github-issue — [appr-061 による再評価] appr-048 時点の推奨 (hub-pull-api) は「正本が Hub の DB にある以上、出口を pull 型にすれば外部書込みの失敗様式が構造的に消える」という理由に基づいていた。この技術的な理由自体は今も正しい。しかし appr-061 で利用者は「GitHub の API キーを使えるので運用としては楽そう」という運用上の理由から GitHub Issue へ戻すことを選んだ。AI が持つ資格情報を Hub 側で新規に発行・失効管理する手間と、既に手元にある GitHub token をそのまま使う手軽さの比較は、実装の綺麗さではなく運用者本人の負担の問題であり、利用者の判断が優先する。ここでは推奨を利用者の決定に合わせて github-issue へ改めたうえで、hub-pull-api が持っていた利点 (外部への副作用が無い) を失う代償を cons/risks に明記して残す。repo-file-export は指示文そのものをファイル化する案であり、appr-063 で採用した「画像だけを commit し本文は Issue に置く」とは別案として区別する。mcp-server は定型処理に対して接続設定の初期コストが釣り合わない、という評価を維持する。なお options は writer 契約により 2-3 件に限られるため、github-issue を加えるにあたり mcp-server を選択肢一覧から外した。落としたのは評価が最下位で、かつ appr-061 の GitHub 回帰でも評価が変わらないためであり、案として否定された記録をここに残す (利点: 会話の中で条件を変えながら要望を探せる / 欠点: 1 指示 = 1 回の取得という定型に対して接続設定と tool 定義の初期コストが釣り合わず、tool 呼び出しの往復が指示文本体に使える文脈を削る)。 (注意: 外部書込みの失敗様式 (孤児・二重起票・再送) を設計で潰す必要がある。submission key による冪等再送と本文ハッシュによる差分判定で対処する (qa-291), GITHUB_ISSUE_TOKEN は Cloudflare Workers Secret に置き、DB・リポジトリ・設定 JSON へ書かない。必要権限は appr-063 により Issues: Read and write + Contents: Read and write, GitHub API クライアントの実装手段 (公式 SDK か fetch 薄クライアントか) は本 decision の範囲外であり、D12 として別立てにした; confidence=medium — 技術的な失敗様式の少なさでは hub-pull-api が優るが、運用負担の比較は利用者本人の状況に依存するため、AI 側から高い確度で断定できる性質のものではない; checked=2026-08-15T01:35:54Z) | github-issue @ 2026-08-16T00:00:00Z | G1, G4, G5 |
| D11 | 改善要望のスクリーンショットを、既存の R2 bucket へ相乗りさせるか、5 本目の専用 bucket を追加するか | confirmed | dedicated-bucket:改善要望スクリーンショット専用の 5 本目 R2 bucket を追加する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'R2 は bucket 数では課金されず、保存容量と操作回数で課金される。想定保存量は無料枠 (storage 10GB-month / Class A 100 万回 / Class B 1000 万回) の内側に収まるため月額 0 円。枠を超えた分は保存容量に応じた従量課金へ移るが、その閾値は 2 案とも同一で差は生じない'} / free=R2 の無料枠 (storage 10GB-month、Class A 100 万回/月、Class B 1000 万回/月) は account 単位のため、bucket を分けても枠の消費は変わらない / fit=保持期間と削除方針が既存 4 bucket と異なる (qa-249 の種別別削除・qa-242 の孤児回収) ため、一括削除の巻き添えを構造的に防げる / pros=保持期間の違うものを混ぜないため、片方の一括削除がもう片方を巻き込まない, binding が分かれ、到達範囲が bucket 単位で限定される, 使用量の監視を用途別に読める / cons=wrangler.jsonc の binding が 1 つ増え、環境ごとの設定項目も増える / risks=環境間で bucket 名の設定漏れが起きると、本番で書込み先が無い状態になる。既存 4 bucket と同じ設定手順に載せることで回避する / lock-in=低 (R2 は S3 互換 API を持ち、オブジェクトの移設は可能) / ops=低 (既存の cron 孤児回収へ 1 対象を加えるだけ) / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/r2/buckets/create-buckets/<br>share-existing-bucket:既存 bucket (docs-cms の画像用など) へ prefix を分けて相乗りする / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'R2 は bucket 数では課金されず、保存容量と操作回数で課金される。想定保存量は無料枠 (storage 10GB-month / Class A 100 万回 / Class B 1000 万回) の内側に収まるため月額 0 円。枠を超えた分は保存容量に応じた従量課金へ移るが、その閾値は 2 案とも同一で差は生じない'} / free=同上 (account 単位) / fit=設定は増えないが、保持期間の異なるものが同居し、削除方針が prefix の扱いに依存する / pros=wrangler の設定が増えない / cons=既存の一括削除や移行作業が改善要望の画像を巻き込む余地が残る, 使用量が用途別に読めない, binding の到達範囲が広くなる / risks=docs-cms 側の運用変更が改善要望のデータに波及する。両者の担当が同じであっても、時期がずれた変更で事故が起きる / lock-in=低 / ops=中 (prefix 単位の削除条件を運用で守り続ける必要がある) / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/r2/buckets/create-buckets/ | dedicated-bucket — R2 は bucket 数で課金されないため、分けることの費用は 0 である。一方で相乗りは、保持期間の違うデータの同居と binding の到達範囲拡大という 2 つの危険を残す。費用が同じなら、事故の余地が小さいほうを採る。 (注意: 環境ごとの bucket 名設定が 1 つ増えるため、preview / production の設定漏れを既存 4 bucket と同じ手順・同じ検査に載せる, 保存量が R2 の無料枠 (10GB-month) を超えると従量課金へ移る。画像は解像度を落とさない前提のため、保持期間と廃棄・削除の運用が費用を決める, bucket を分けても account 単位の無料枠は共有されるため、費用の観点では相乗り案との差は生じない; confidence=high — R2 の課金単位に bucket 数が含まれないことを公式料金表で確認済みで、費用差なしの前提が確定している。残る判断は運用の分離だけで、結論は安定している; checked=2026-08-15T01:24:46Z) | dedicated-bucket @ 2026-08-15T01:24:46Z | G4, G5 |
| D12 | GitHub の Issue 起票・更新・検索と、appr-063 で加わった Contents API による画像・診断ファイルのcommit を、Cloudflare Workers からどの手段で行うか。公式 SDK (Octokit) を使うか、REST を fetch で直接叩く薄いクライアントを自前で持つか (経緯: 旧 D10 が同じ問いを扱い fetch-thin-client で確定していたが、appr-048 のGitHub 出口廃止に伴い撤回された。appr-061 で出口が GitHub へ戻り、appr-063 で用途がContents API へ広がったため、問いを別 id で立て直す) | confirmed | fetch-thin-client:REST を fetch で直接叩く薄いクライアントを自前で持つ / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '使う endpoint は Issue の作成・更新・検索と Contents の GET/PUT の 5 本程度。自前実装でも数百行に収まり、依存の追随作業が発生しない'} / free=GitHub API の認証済みレート制限 (5,000 requests/hour) / fit=必要な endpoint が少数かつ固定であるため、汎用 SDK の網羅性が利点にならない。Workers の fetch をそのまま使え、bundle も増えない / pros=Workers ランタイム互換の心配が無い (Node 固有 API に依存しない), bundle size が増えず、cold start に影響しない, 依存更新の追随が不要 / cons=レート制限のハンドリング・リトライ・ページングを自前で書く必要がある, API の仕様変更に自分で追随する必要がある / risks=Contents API の更新は既存 blob の sha を要求するなど、endpoint 固有の作法があり、自前実装では取りこぼしやすい。契約テストで固定する / lock-in=低 (自前コードなので差し替えが容易) / ops=低 (依存追随なし。ただし API 仕様変更の追随は自分で行う) / evidence=https://docs.github.com/en/rest/issues/issues, https://docs.github.com/en/rest/repos/contents<br>octokit-rest:公式 SDK の @octokit/rest を使う / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '依存の追加と定期更新が乗る。実装量は最小'} / free=GitHub API の認証済みレート制限 (5,000 requests/hour) / fit=全 endpoint に型付きメソッドが用意され、ページングやリトライの実装が済んでいる。ただし今回使う endpoint は 5 本程度で、網羅性が効かない / pros=型付きで endpoint の作法 (Contents の sha 要求など) を取りこぼしにくい, レート制限・リトライ・ページングが標準で入っている / cons=bundle が大きく、Workers の cold start と size 上限に影響しうる, 依存の定期更新が運用に乗る / risks=Workers ランタイムでの動作は plugin 構成によっては Node 互換 flag を要する。採用する場合は事前に実機で確認が要る / lock-in=中 (SDK の API 形に実装が寄る) / ops=中 (依存追随) / evidence=https://github.com/octokit/rest.js<br>octokit-core:@octokit/core (最小構成) に必要な plugin だけを足して使う / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '@octokit/rest より依存が小さいが、必要な plugin の選定と組立てが要る'} / free=GitHub API の認証済みレート制限 (5,000 requests/hour) / fit=認証とリクエスト整形だけを借り、endpoint は自分で指定する。自前実装と SDK の中間に位置する / pros=認証ヘッダ・エラー整形といった定型を借りられる, @octokit/rest より bundle が小さい / cons=型付きメソッドが無いため、endpoint 固有の作法は結局自分で調べる, plugin 構成の判断が増え、fetch 直叩きより設定が複雑になる / risks=中間解であるため、SDK の利点も自前の軽さも中途半端になりうる / lock-in=低〜中 / ops=中 (依存追随 + plugin 構成の維持) / evidence=https://github.com/octokit/core.js | fetch-thin-client — 使う endpoint は Issue の作成・更新・検索と Contents の GET/PUT の 5 本程度に固定されており、SDK の網羅性が利点として効かない。一方 Workers 上では bundle size と Node 互換が常に判断材料になるため、依存を増やさない側が扱いやすい。cons に挙げたレート制限・ページングも、今回の用途 (1 日数十件・単一リポジトリ) では自前で扱える規模に収まる。risks に挙げた Contents API の sha 要求など endpoint 固有の作法は、qa-304 で定めた「GET してから作成/無変更/更新を分岐する」手順を契約テストで固定することで抑える。旧 D10 でも利用者は同じ問いに対して fetch-thin-client を選んでいた。 (注意: Contents API の更新は既存 blob の sha を要求する。自前実装ではここを取りこぼしやすいため、GET → 作成/無変更/更新の分岐を契約テストで固定することを実装条件に含める, octokit-rest を選ぶ場合は、Workers ランタイムでの動作を実機で先に確認する必要がある; confidence=medium — endpoint 数が少ない前提が崩れる (GitHub 側の機能を広く使い始める) と評価が変わる。現時点の用途に限った推奨である; checked=2026-08-16T00:00:00Z) | fetch-thin-client @ 2026-08-16T13:16:00+09:00 | G1, G4, G5 |
