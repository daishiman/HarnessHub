---
graph_node_id: "spec-post-signin-landing-observability"
artifact_kind: "specification"
artifact_subtypes: []
project_id: "harness-hub"
domain: "platform"
tags: ["post-signin","landing","observability","auth","runtime-env","web-only","frontend","ui-ux","infrastructure","testing-qa"]
priority: "high"
start_date: null
target_date: null
iteration: null
title: "Harness Hub サインイン後 着地不達の原因究明と遷移経路の継続検証 仕様追補"
owners: ["daishiman"]
created_at: "2026-08-07T10:59:42Z"
updated_at: "2026-08-07T10:59:42Z"
status: "draft"
depends_on: ["spec-harness-hub-requirements","spec-post-signin-workspace-scope"]
related_nodes: ["spec-harness-hub-requirements","spec-post-signin-workspace-scope","arch-harness-hub-frontend","arch-harness-hub-security","arch-harness-hub-infrastructure","arch-harness-hub-testing-qa"]
resource_scope: ["specs/harness-hub-post-signin-landing-observability-addendum.md","system-spec/frontend.md","system-spec/ui-ux.md","system-spec/infrastructure.md","system-spec/testing-qa.md","system-spec/maintenance-ops.md"]
purpose: "サインイン後に稼働状況しか表示されない不具合について、安全側の縮退が観測できず静かな全面停止と区別できない状態を是正し、原因を事後に判別できる状態を作る (本件の原因は qa-198 で確定した = 本番の稼働ビルドが着地先を直した commit 150a0f14 を含んでいなかった。是正はコード変更ではなく再デプロイ。本追補が固定するのは、その確定に 10 ラウンド以上を要した観測不能状態のほうである)"
goal: "qa-170〜qa-199 と appr-034 / appr-035 / appr-037 / appr-038 / appr-039 を実装計画が参照できる単一の仕様境界として維持し、再発を CI が検出できる状態にする"
scope_in: ["着地不達の原因を事後に判別可能にする仕組み (縮退の記録点の統一)","環境値の解決層の一本化と module 最上位での構築禁止","縮退した事実と解決できなかった名前の記録 (値は記録しない)","既定着地 /dashboard と着地画面の内容 (利用者の直接決定)","分類語彙の列挙一致を到達可能性ベースで検査する仕組み","縮退の観測可能性を検査する仕組み","既存の deploy 時ゲート (必須 secret の実投入検査) の限界の明記と回帰防止","稼働中の成果物と repository の commit の対応を認証なしで確認できる仕組み (build 同一性)","認証成功したまま着地先が既定値へ後退した事象を、認証失敗と区別して記録する仕組み","本番の稼働ビルドが既定 branch の HEAD より古い状態が続くことの検出"]
scope_out: ["相対 path 検証・open redirect 防止・scope 解決の契約変更 (先行仕様のまま)","S09 ダッシュボードの KPI・推移・完了率・ランキング・部門別削減 (P5 据え置き)","authorize() の判定順・role 判定の変更","検査 script の実装そのもの (task 工程)"]
acceptance: ["認証を含む全ての環境値読み出しが既存の吸収層を通り、吸収層外の直接読み出しが 0 件である","module 最上位で環境値に依存する構築を行っていない","縮退時に、縮退した事実と解決できなかった名前が記録され、値は記録されない","遷移元が無いサインイン成功で /dashboard へ着地し / に留まらない","着地画面が所属テナント／ワークスペースを常時表示し、複数所属ならその場で切り替えられる","着地画面が自分が最後に触ったものを種別横断で提示し、空状態でも次の行動への導線を示す","着地画面が既存業務画面群への導線を持ち行き止まりにならない","稼働状況が通常時に表示されず、稼働状況が主役の画面が既定着地になっていない","scope 未解決時に生の 403 でなく回復手段が提示される","認証基盤が使えない状態が利用者の操作ミスに見える文言で表示されない","権限不足 (403) で再サインインへ誘導せずループを作らない","分類語彙の収集が到達可能性ベースで行われ、宣言外の app/package が増えたら検査が落ちる","実測で見落とした 8 件の fixture 全てで検査が発火することが test で固定されている","分類語彙の収集が型宣言・zod 導出・ORM schema 導出の 3 経路すべてを対象にしている","同一のリテラル union が複数箇所に独立定義されている状態を検査が検出し、型宣言・zod・drizzle schema の 3 経路すべてを情報源として突合する","認証に関わる構築物が module scope に保持されず、リクエストごとに解決される","縮退の記録が、テナント未解決・テナント非 active・OIDC 接続未登録・環境値未解決 (名前)・cookie 不在・署名検証失敗の 6 種を互いに区別できる","段0 の分類語彙に tenants.status (active/suspended) が含まれ、検査が実際に発火する","既存の deploy 時ゲートが維持され、必須名を 1 つ外した状態で deploy が実際に失敗する","認証系の環境値について、投入の有無と実装が読める経路かの両方が別々に検査される","初回 deploy 相当 (secret 未投入の新規 Worker) でも deploy が不足名の列挙で失敗し、preview Worker が検査対象から外れていない","縮退の記録に、どの経路・どの environment から環境値を解決したかが含まれる","現在の middleware.ts / app/page.tsx / lib/authz/runtime.ts の 3 箇所すべてに対して環境値規律の検査が実際に発火する","既定着地の値がテストで複製されず実装定数を import している","稼働中の成果物から、それが repository のどの commit に対応するかを認証なしで確認できる (V6)","認証は成功したが着地先が既定値へ後退した事象が、認証失敗とは区別して記録される (V2)","本番の稼働ビルドが既定 branch の HEAD より古い状態が続いていることを検出できる (V7)"]
architecture_refs: ["arch-harness-hub-frontend","arch-harness-hub-security","arch-harness-hub-infrastructure","arch-harness-hub-testing-qa"]
parent_feature: null
feature_package_id: null
phase_ref: null
file_path: "specs/harness-hub-post-signin-landing-observability-addendum.md"
template_id: "specification"
template_version: "1.0.0"
confirmation_status: "confirmed"
evaluation_status: "fail"
confirmation_evidence: {"evaluated_digest":"e1ecf64f6bd0dfc66926fc252aae33dd70303563a0bfda48954e3f58f64a9146","evaluator":"assign-system-spec-completeness-evaluator","evidence_ref":"system-spec/completeness-report.json"}
source_lineage: {"imported_at":"2026-08-07T10:59:42Z","origin_kind":"system-spec-harness","source_digest":"e1ecf64f6bd0dfc66926fc252aae33dd70303563a0bfda48954e3f58f64a9146","source_path":"system-spec/spec-state.json","source_plugin":"system-spec-harness","source_version":"0.1.0"}
classification_confidence: 0.99
classification_reason: "qa-170〜qa-199 / appr-034〜appr-039 の確定契約を横断参照する製品仕様追補"
classification_candidates: [{"artifact_kind":"specification","candidate_path":"specs/harness-hub-post-signin-landing-observability-addendum.md","confidence":0.99},{"artifact_kind":"architecture","candidate_path":"architecture/harness-hub-infrastructure.md","confidence":0.42}]
issue_linkage: null
tracker_binding: "none"
beads_linkage: null
github_publication: {"labels":[],"milestone":null,"mode":"local_only","project_aliases":[]}
github_project_linkages: []
pull_request_linkages: []
execution_contexts: []
completion_evidence: {"completed_at":null,"evidence_refs":[],"policy":"manual","reconciled_at":null,"source":null,"status":"not_applicable"}
implementation_readiness: {"checked_at":"2026-08-07T10:59:42Z","missing_sections":[],"status":"complete"}
---

# Harness Hub サインイン後 着地不達の原因究明と遷移経路の継続検証 仕様追補

## 0. この文書は何か

サインイン後に業務画面へ到達できず「稼働状況 / Hub の実行基盤が起動しています」だけが表示される不具合について、**なぜそうなっているのか**を追跡し、**同じことが再発したときに気付ける仕組み**を製品契約として固定する。

**原因は確定した (§2.10)。本番で動いていたビルドが、着地先を直した commit を含んでいなかった。**認証は成功しており、signin ページが送っていた戻り先 `/` へ正しく着地していた。是正はコード変更ではなく再デプロイに尽きる。

**それでも本書の主題は原因ではなく、原因を判別可能にする仕組みのほうである。** 1 回の GET と 1 行の grep で決まる事実の確定に 10 ラウンド以上を要したこと自体が、「稼働中のビルドがどの commit か」と「着地先が既定値へ後退した事実」のどちらも観測できない状態の代償である。§2.10 末尾の V6 / V2 / V7 がその是正にあたる。

先行する [Workspace スコープ導線 仕様追補](harness-hub-post-signin-workspace-scope-addendum.md) (2026-08-02 確定) は同じ症状を扱い、契約 A (サインイン済みで `/` に来たら着地先へ送る) を定めた。**その契約は実装済みだったが、症状は解消しなかった**。本書はその理由を扱う。

正本は `system-spec/spec-state.json` の qa-170〜qa-199 (原因の確定は qa-198、停止条件の緩和承認は appr-039)。本書はそれを実装計画が参照できる単一の仕様境界へ束ねたものであり、内容を複製せず要点と根拠を記す。

**独立監査の結果も本書に残す。** 完成度評価 (`system-spec/completeness-report.json`) の総合判定は **FAIL** である。内訳は「過去に起きた事実の記録」「plugin 側で直すべき課題」「実装工程でやる作業」のみで、ヒアリング工程の側に打てる手は残っていない (独立監査 C06 の 2 インスタンスが一致して確認)。この状態のまま以降の工程へ進む判断は利用者のものであり、appr-039 に逐語で保存してある。

## 1. 症状

| 観測 | 内容 |
|---|---|
| 入口 | `https://harness-hub.daishimanju.workers.dev/harness-hub/signin` でサインイン画面は表示される |
| 通過後 | `https://harness-hub.daishimanju.workers.dev/#main` に着地する |
| 表示 | 「稼働状況 / Hub の実行基盤が起動しています / 依存先を含む死活状態は /health で確認できます。」のみ |
| 期待 | ダッシュボードや詳細画面などの業務画面 |

URL 末尾の `#main` は `apps/hub/src/app/layout.tsx` のスキップリンク (`<a href="#main">本文へスキップ</a>`) の断片であり、症状とは無関係である。混乱の元になるため明記しておく。

## 2. 原因 (確定済み。本節には確定に至るまでの反証の経緯も残す)

> **本節の位置づけは調査の途中で 3 度変わった。** 当初は「原因を実測で特定した」と書き、次にその機序が**いずれも反証されて**未特定へ戻り、最終的に本番への 2 回の観測で確定した。確定した原因は §2.10 にある。**2.3〜2.6 に残した反証の経緯は削除していない。** 誤った推論そのものが、本文書が要求している仕組み (V2/V6/V7) の必要性の根拠だからである。特に 2.4「残っている候補」は、**そこに挙げた候補のいずれもが真の原因ではなかった**。消去法で絞り込んだ候補を観測せずに原因と呼ぶ危うさの実例として残す。

### 2.1 契約 A は実装されていた (この事実は変わらない)

`apps/hub/src/app/page.tsx` は、session 秘密が読めてかつ有効な session cookie があれば `DEFAULT_POST_SIGNIN_LANDING` へ redirect する。仕様どおりである。

着地先は `apps/hub/src/lib/routing/post-signin-landing.ts` の `DEFAULT_POST_SIGNIN_LANDING = '/sheets'` で、`src/app/(dashboard)/sheets/page.tsx` として**実在する**。すなわち「着地先が未実装だから飛べない」ではない。

### 2.2 認証の環境値は 3 箇所から独立に読まれている

このリポジトリには、Cloudflare Workers 上で環境値を読むための吸収層が既にある。

```
apps/hub/src/app/health/runtime-env.ts
  → @opennextjs/cloudflare の getCloudflareContext().env から読み、
    Workers ランタイム外では process.env へフォールバックする
```

`lib/tenant-data/runtime.ts` と `lib/publish/runtime.ts` はこの層を使っている。一方、**認証に関わる 3 箇所は吸収層を通らず `process.env` を直接読む**。

| 読み出し箇所 | 経路 | 失敗時の見え方 |
|---|---|---|
| `app/health/runtime-env.ts` | `getCloudflareContext().env` → fallback `process.env` | — |
| `lib/tenant-data/runtime.ts` | 同上 | — |
| `lib/publish/runtime.ts` | 同上 | — |
| **`middleware.ts:26-35`** | `process.env` 直読み。しかも **module 最上位** | provider を差さない = deny-all。理由は残らない |
| **`app/page.tsx`** | `process.env` 直読み | redirect せず稼働状況を表示。理由は残らない |
| **`lib/authz/runtime.ts:220`** | `authRuntime(source = process.env)` | 例外 → サインイン画面が「認証基盤が未結線です」を表示 |

3 つ目は調査の終盤で見つかった。**当初この文書は読み出し箇所を 2 箇所と書いていた。実装より狭い列挙であり、分類語彙で 8 回繰り返したのとまったく同じ型の誤りが、検査対象の列挙でも起きていた。**

### 2.3 反証された 2 つの機序

**機序 1 (撤回)**: 「Workers は secret を `process.env` へ供給しないため、投入済みでもゲート緑・`middleware` からは `undefined`」

反証: Cloudflare 公式 changelog (`2025-03-11-process-env-support`) により、`nodejs_compat` 有効かつ `compatibility_date` が 2025-04-01 以降なら `nodejs_compat_populate_process_env` が既定で有効になり、secret は **module 最上位を含む任意の scope** で `process.env` から読める。`apps/hub/wrangler.jsonc:12,14` は `compatibility_date: "2025-09-23"` / `compatibility_flags: ["nodejs_compat"]` であり、この条件を満たす。**この機序は本リポジトリでは成立しない。**

**機序 2 (撤回)**: 「Next.js の edge ビルドが `process.env.X` をビルド時にインライン化し、CI build 時点で未設定の値が `undefined` として焼き込まれる」

反証: `AUTH_SESSION_SECRET` を env から除いた状態で `pnpm run build:next` を実行し、生成物を直接確認した。

```js
// .next/server/src/middleware.js (実測)
let iz=process.env.AUTH_SESSION_SECRET,iA=process.env.AUTH_ACCESS_TOKEN_SECRET,...
```

**インライン化されず、実行時のプロパティ参照として残っている。** `.next/server/edge-runtime-webpack.js` にも `process` の shim は含まれない (1504 bytes、grep 0 件)。したがって `process` は host runtime のものであり、`nodejs_compat` の populate 対象になる。**この機序も成立しない。**

### 2.4 残っている候補 (いずれも本番の観測なしには判別できない)

1. **現在稼働している本番 Worker が deploy 時ゲートを通っていない版である** — 2026-08-02 より前の deploy、または手元からの `wrangler deploy` (2.9)
2. **isolate の再利用による stale 値の焼き付き** — 下記のとおり、機序としては**公式記述で成立が確認された**
3. **認証以外の原因** — 具体的には `tenants.status !== 'active'`。下記のとおり実体が特定された

**候補 2 について (qa-187)**: Cloudflare 公式 [Workers Best Practices](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/) は、**binding だけを変更しコードを変更しない deploy では isolate が再利用されることがある**と明記し、env 由来の値を global scope のインスタンスへ保持すると「基盤の binding が変わっても stale な値のまま持続し得る」ことを名指しの anti-pattern として警告している。正しい形は「リクエストごとに新しいインスタンスを作る」ことである。あわせて [process ドキュメント](https://developers.cloudflare.com/workers/runtime-apis/nodejs/process/)より、lazy populate は `process` への**最初のアクセス時点**で一度きり行われ、投入有無を再評価しないことが確認できる。

`middleware.ts:26-35` は module 最上位で読んだ値から `authAdapter` を構築し module scope に保持する。module 最上位は isolate 起動ごとに 1 回しか評価されない。**この形は公式が警告している anti-pattern と構造的に一致する。** ある isolate が secret 未投入の時点で `process` に触れて `undefined` を populate すると、その後 secret を投入しても、その deploy が binding のみの変更であれば同じ isolate が再利用され、deny-all 版がそのまま使われ続ける。isolate が evict されるまで解消しない。

ただし**本番でこれが起きたとは断定しない**。断定するには当該 isolate の生成時刻と secret 投入時刻の前後関係という本番の実測が要り、本セッションでは取得していない。記録の強さは「機序として確認済み・実際の発生は未確認」である。

**候補 3 について (qa-188)**: `packages/db/schema/core/identity.ts:14` の `tenants.status` (`text('status', {enum: ['active','suspended']})`) が、`apps/hub/src/lib/auth/db-ports.ts:306-308` の `enabled: tenant.status === 'active'` を通じて `resolveTenantOidcConfig` の判定に使われている。同箇所のコメントは「停止中テナントは接続を解決させない。認証の入口で閉じる方が安全側」と述べている。

つまり **`status` が `suspended` であるだけで、秘密が正しく投入され正しく読めていても同じ症状になる**。この経路は秘密の投入状態と完全に独立している。「秘密を直せば直る」と決め打つと外し得る。候補 3 を候補 1・2 と同格で残す理由がここで具体化した。

### 2.5 本当の欠陥 (ここは反証されていない。むしろ強まった)

問題は縮退したことではなく、**縮退したことが誰にも分からないこと**である。エラーも 5xx も警告も出ず、正常に見える画面が返る。

`middleware.ts` の deny-all 自体は正しい設計である。実装のコメントが述べているとおり、「秘密が無いときは検証を飛ばす」実装にすると設定漏れがそのまま認証バイパスになる。**安全側に倒すこと自体は妥当**だ。

> 安全側の縮退は、観測できなければ静かな全面停止と区別がつかない。

**そして本セッションそのものが、この欠陥の実証になっている。** 公式文書を当たり、リポジトリを読み、ビルドを実行し、独立監査を 2 系統回してなお、私は「本番で何が起きているか」を言えていない。本番へ curl を打てないからではない。**打てたとしても、2.4 の 3 候補すべてで外から見える応答はほぼ同じになる**からである。3 経路とも安全側に倒れ、3 経路とも「どの名前が解決できなかったか」を残さない。

なお 3 経路のうち `lib/authz/runtime.ts` 経由だけは、サインイン画面に「認証基盤が未結線です」と表示する。ADR §10「認証基盤が未結線であることを画面上でも隠さない」の実装であり、同ファイルは in-memory 実装を本番へ差さない理由も「未結線が 200 応答で隠れる」と明記している。**この設計思想は既にリポジトリにある。** E-3 が求めているのは新しい思想ではなく、既にある思想を残り 2 経路へ及ぼすことである。

### 2.6 本セッションで確認できなかったこと

本番 URL への直接アクセス (curl) は本セッションで許可されなかった。したがって以下は**未確認**である。

- 本番環境で秘密が未投入なのか、投入済みだが解決できていないのか、そもそも認証以外が原因なのか
- 縮退がいつから発生しているのか

このため実装順序を **観測 (2.7 の E-3) を先、修正 (E-1 / E-2) を後** とする。原因が特定できていない以上、これは選択ではなく必然である。判別手段を持たないまま修正を入れると、直ったのか症状が別の形に移ったのかを区別できない。

### 2.7 契約

- **E-1 環境値の解決層を 1 つにする**。認証を含む全ての環境値読み出しは既存の吸収層を通す。認証だけが例外である理由は無い。
- **E-2 module 最上位で環境値に依存する構築をしない**。リクエスト context の外で確定させると、後から供給されても反映されない。根拠は 2 本ある: (a) Workers は request context 外の I/O を許可しない (公式)、(b) binding のみの deploy では isolate が再利用され、global/module scope に保持した env 由来の値は stale なまま持続し得る (公式が名指しで警告する anti-pattern・2.4 候補 2)。認証に関わる構築物は module scope に保持せず、リクエストごとに解決する。
- **E-3 縮退した事実と、どの名前が解決できなかったかを記録する**。値は決して記録しない (名前のみ)。記録の契機はリクエスト毎ではなく、解決状態が変化したときと、縮退中に拒否が起きたとき。どの environment で解決したかも記録する (値は記録しない)。記録は次の 6 つを**互いに区別できる形**で残す (qa-188-c):
  1. テナントが解決できない (slug 不一致)
  2. テナントは在るが `status` が `active` でない
  3. OIDC 接続が登録されていない
  4. 環境値が解決できない (**どの名前か**を記録)
  5. cookie が無い
  6. 署名検証に失敗した

  現状この 6 つが利用者から見てほぼ同一の出力へ潰れていることが、本 feature の欠陥の中身である。値・token・cookie の中身・claim 本体・個人データは記録しない (qa-151 [147-b] 非記録契約)。
- **E-4 必須の秘密が投入されていない状態を本番へ出さない — これは新設ではなく、既に存在する**。名前だけを宣言し値は設定ファイルへ入れないという方式は [Cloudflare Workers 公式](https://developers.cloudflare.com/workers/configuration/secrets/)が示すとおりだが、本リポジトリはこれを 2026-08-02 (`HarnessHub-o2i.13`) に既に land 済みである。詳細は 2.8 に記す。仕様が求めるのは新設ではなく、**既存ゲートの限界の明示と回帰防止**である。

### 2.8 秘密が読めない状態は目視で判別できない

Cloudflare の secret は書込専用で、投入後は Wrangler からも dashboard からも値を読み戻せない。したがって「投入済みだが読めていない」のか「未投入」なのかは、**値を見る方法では原理的に判別できない**。6 章の未解決事項 1 がこの仕様上、E-3 の観測を実装する以外に解けないのはこのためである。

なお `@opennextjs/cloudflare` の公式 how-to には「環境変数は `process.env` でのみ利用可能」という記述がある。これは `next dev` 配下の **local 開発の文脈**を指すと解するのが Workers の binding モデルからの演繹として妥当だが、**この解釈は一次ソースの逐語では未確認である** (未解決事項 5)。ただし本文書の結論はこの解釈に依存しない。2.3 で見たとおり、本リポジトリの compat 設定では `process.env` から secret は読める。したがって **E-1 / E-2 の根拠は「読めないから」ではない** — E-2 は module 最上位の評価が isolate 生成時の 1 回きりで再評価されないこと、E-1 は縮退の記録点を 1 つに束ねることが根拠である (2.7)。**欠陥は吸収層の設計ではなく、認証に関わる 3 箇所がそこを通っておらず、倒れた理由を残さないことに限定される**。

また Workers は `cloudflare:workers` から `env` を import すればリクエスト処理の外からも secret を読める。これは E-1 の実装選択肢を増やすが、**E-2 を免除しない**。公式は同時に **Workers がリクエスト context の外での I/O を許可しない**ことも明記しており、module 最上位で認証基盤を組み立てる設計はこの制約と正面から衝突する。読めることと、読んだ結果をプロセス生存期間ずっと固定してよいかは別の問題である。

### 2.9 deploy 時ゲートは既に存在する。それでもこの症状は起きた

本文書の草稿は当初、E-4 を「新しく追加すべき契約」として書いていた。これは誤りだった。実測すると、同等のゲートは 5 日前の 2026-08-02 (`HarnessHub-o2i.13`) に既に land している。

- `docs/infrastructure-spec.md` L76 — 機械可読な台帳 `scripts/ci/worker-secrets-registry.json` を正本とし、`scripts/ci/check-worker-secrets.mjs` が **台帳 ↔ `apps/hub/wrangler.jsonc` の `secrets.required` ↔ 本番の実投入** を三方向で突合する
- `apps/hub/wrangler.jsonc` — `secrets.required` に `AUTH_SESSION_SECRET` を宣言済み
- `.github/workflows/ci.yml:249` — `check-worker-secrets.mjs --live` を `wrangler deploy` **より前**の step に置く。違反 1 件で exit≠0 となり deploy step へ到達しない

さらに台帳は `AUTH_SESSION_SECRET` について「未投入なら middleware が session provider を差さず deny-all のままになる (認証バイパスにはならないがログインが一切通らない)」と書いている。**本文書 1 章の報告症状そのものが、5 日前に別の文書へ既に書かれていた。**

では、なぜ防げなかったか。**ゲートが検査する層と、実装が読む層が違うからである。**

`--live` が確かめるのは Cloudflare 側 Worker に secret が**投入されているか**であり、取得できるのは `wrangler secret list` の返す**名前だけ**である (値は write-only で読み戻せない)。**このゲートは「その名前が入っている」ことしか言えず、「実装がそれを読めた」ことは何も言わない。**

草稿はここから「Workers は `process.env` へ供給しないから読めていないのだ」と続けていたが、その事実前提は 2.3 で反証された。したがって**なぜ防げなかったかは、現時点では答えられていない**。答えられているのは、ゲートが答えられる範囲の狭さだけである — 投入されていることと、実装がそれを読めることは、別々に確かめるべき 2 つの事実であり、E-4 は前者しか見ない。後者を見るのは V6 であり、そのための前提を作るのが E-1 / E-2 / E-3 である。

この節を書いた時点では、ここから「秘密は投入済みであり、残るのは読めていない側だけ」と結論した。**その結論は 2.3 で反証された。** 現在言えるのは次の 2 点に留まる。(1) 本番 Worker が現行 CI 経路で deploy されたものであれば `AUTH_SESSION_SECRET` は投入されている。(2) その前提自体を、本番への curl が許諾されていない以上リポジトリの静的読取だけでは確定できない — 手元から `wrangler deploy` された版が残っていればゲートは一度も通っていない。この判別を不要にするのが E-3 であり、原因が 3 候補に開いたままである現状は、E-3 を先に実装する順序の根拠をむしろ強めている。

なお `docs/infrastructure-spec.md` L77 は、このゲートを作った契機を「`AUTH_ACCESS_TOKEN_SECRET` が未投入のまま本番が稼働していた。発覚が遅れたのは middleware が fail-closed だったため — 鍵が無いとき `principal=null` に倒す設計は正しいが、副作用として『鍵が無い』と『token が不正』が同じ 401 へ潰れ、設定漏れが障害として立ち上がらなかった」と記録している。**本文書 2.4 が「本当の欠陥」として特定した機序は、同じリポジトリが 5 日前に別の secret について既に書き残していた機序と同型である。**同じ機序が別の入り口から二度成立したという事実こそが、E-3 (縮退そのものを観測可能にする) を単発の是正ではなく恒久契約として置くべき理由である。

### 2.10 確定した原因 — 本番が古いビルドのまま動いていた

2 回の観測で確定した。

```
$ curl -s https://harness-hub.daishimanju.workers.dev/api/auth/harness-hub/csrf
$ curl -s -X POST .../api/auth/harness-hub/signin/tenant-oidc \
      --data-urlencode "csrfToken=$T" --data-urlencode "callbackUrl=/sheets"
  → HTTP/2 302
    location: https://accounts.google.com/o/oauth2/v2/auth?...

$ curl -s https://harness-hub.daishimanju.workers.dev/harness-hub/signin | grep -o 'callbackUrl[^>]*'
  → callbackUrl" value="/"
```

**1 つ目でサインインの入口が正常だと分かり、2 つ目で原因が確定した。**

本番の signin 画面は、サインイン後の戻り先 (`callbackUrl`) として `/` を送っている。認証は成功しており、Auth.js はその指示どおり `/` へ戻していた。**失敗していたのではなく、指示された場所へ正しく着地していた。**

この値は履歴と一意に照合できる。

| commit | 日時 | `callbackUrl` の値 |
|---|---|---|
| `43e06e10` | 2026-07-30 14:11 | `value="/"` を**定数としてベタ書き** |
| `150a0f14` | 2026-08-03 13:02 | `DEFAULT_POST_SIGNIN_LANDING` (= `/sheets`) 由来へ置換 |

本番の実測は前者と一致し、後者と一致しない。**本番で動いているビルドは `150a0f14` を含んでいない。**

#### 是正の内容 — 新しいコードは要らない

`150a0f14` は 2 重の是正を既に入れている。

1. signin form の `callbackUrl` を `/sheets` にする
2. `app/page.tsx` (`/` そのもの) に、有効な session cookie があれば `DEFAULT_POST_SIGNIN_LANDING` へ redirect する処理を足す

2 があるため、古い cookie で `/` に来た利用者も業務画面へ送られる。**是正は「`150a0f14` 以降を本番へ deploy する」ことに尽きる。**

#### なぜ確定まで 10 ラウンド以上かかったのか

最終的には 1 回の GET と 1 行の grep で決まった。それまで迷走したのは、次の 2 つが観測できなかったからである。

- **稼働中のビルドがどの commit に対応するかを知る手段が無い。** そのため「コードは直っている」と「本番が直っている」を区別できず、**コードを読めば読むほど誤った確信が強まる**状態になっていた。実際、原因として挙げた機序は 2.3〜2.6 のとおり全て反証された。
- **着地先が既定値へ落ちた事象を記録する手段が無い。** 認証に失敗すればサインイン画面へ戻るので気づける。しかし「認証は成功したが、意図しない場所へ着地した」は**成功として通過し、痕跡が残らない**。

この 2 点はそのまま、本文書が要求する V2 / V6 / V7 の存在理由である。したがって受入基準へ次を加える。

| 検証 | 追加する要求 | 本件での効果 |
|---|---|---|
| V6 | 稼働中の成果物から、対応する commit を**認証なしで**確認できること | 切り分けが 1 回の GET で終わる |
| V2 | 「認証成功・着地先が既定値へ後退」を、認証失敗と**区別して**記録すること | 成功として通過する異常を可視化する |
| V7 | 稼働ビルドが既定 branch の HEAD より古い状態が続くことを検出すること | 本件は修正から 4 日間 (08-03 → 08-07) 未反映だった |

## 3. 着地先と着地画面 (利用者の直接決定)

### 3.1 既定着地 = `/dashboard` (appr-034)

独立監査が「既定着地 `/sheets` は利用者の承認にも AI への委任範囲にも紐付いていない」と指摘したため、利用者へ 3 択を提示し直接決定を得た。

| 選択肢 | 開示したコスト |
|---|---|
| `/sheets` を維持 | 実装量の増加なし |
| `/catalog` | 業務ツール一覧へ着地 |
| **`/dashboard` を先に作る (選択)** | 稼働状況・最近の作業をまとめた画面を新規に作る。S09 で予定されている段階の前倒しになり実装量が増える |

利用者は実装量増を了解のうえ `/dashboard` を選択した。

**前倒しするのは S09 の全体ではない。** S09 (KPI・推移・完了率・ランキング・部門別削減) は P5 のまま据え置き、着地に必要な部分だけを先に作る。詳細は `docs/frontend-spec.md` §10。

### 3.2 着地画面の内容 (appr-035)

独立監査は続けて「着地先の選択は承認されたが、その画面が何を表示するかは委任範囲外なのに AI が代理確定している」と指摘した。全選択肢のコストを対称に開示したうえで利用者へ 3 問を提示し、以下を得た。

| # | 論点 | 決定 | 権限 |
|---|---|---|---|
| a | 所属コンテキスト | 常時表示し、複数所属ならその場で切り替えられる | 利用者本人 |
| b | 行き止まり回避 | 既存業務画面群への導線を必ず持つ | AI 代理 (選択肢が一意) |
| c | 最近の作業 | 自分が最後に触ったものを種別横断で更新日時順に | 利用者本人 |
| d | 稼働状況 | 通常時は出さず、異常時だけ出す | 利用者本人 |
| e | scope 未解決時 | 生の 403 でなく回復手段を提示する | AI 代理 (選択肢が一意) |
| f | 認証基盤不可用時 | 利用者の操作ミスに見える文言で表示しない | AI 代理 (選択肢が一意) |

**d は本不具合の直接の是正である。**「稼働状況しか出ない」は稼働状況が主役に据えられた画面へ縮退した結果であり、利用者が d を選んだことで **「稼働状況が主役の画面は正常な着地先ではない」が仕様要求として確定した**。

f は権限不足 (403) で再サインインへ誘導しない契約と同根である。誘導するとループになり、利用者は永久に業務画面へ到達できない。

### 3.3 3.2 の決定を取るときに何を見せたか (記録上の開示)

独立監査が「AI が『(推奨)』ラベルを付けて誘導していないか、記録からは検証できない」と指摘した。事実を開示する。

**実際に提示した 3 問とも、先頭の選択肢に「（推奨）」ラベルが付いていた。そして 3 問とも先頭が選ばれた。** さらに 3 問とも、先頭選択肢の説明を「今回の症状を直接解決する」という同じ言い回しで締めていた。選択肢ごとのコストとトレードオフは対称に開示していたが、この 2 点の偏りは残る。

そのうえで 3.2 の決定は取り消さない。コスト開示は対称で、利用者は実装量が増える側を了解して選んでいる。とくに d (稼働状況は異常時だけ) は、利用者が最初に報告した症状の是正そのものであり、推奨が無くても同じ結論に至る性質のものである。**ただし本節を読んで再考したい場合、いずれの決定も差し戻せる。この開示は追認を求めるものではない。**

以後、提示した質問文は推奨ラベルの有無を含めて逐語で記録する。AI の要約だけでは、後から誰も中立性を検証できない。

### 3.4 変更しないもの

先行仕様の契約 (相対 path のみ許可・open redirect 防止・認証済みで `/` に来たら着地先へ・scope 解決・所属 workspace が 1 件なら選択画面を挟まない) は一切変更しない。**既定着地の値だけ**が変わる。

実装上の唯一の正本は `apps/hub/src/lib/routing/post-signin-landing.ts` の `DEFAULT_POST_SIGNIN_LANDING`。テストは値を書き写さずこの定数を import する。二重管理でずれると、検査が症状を見逃す側に倒れる。

## 4. 継続検証の仕組み (再発に気付くための CI 検査)

原因究明だけでは同じことが起きる。以下を CI の検査点として置く。

### V2 着地先の非退行

既定着地が業務画面であり続けることを検査する。実装定数を import し、リテラルを複製しない。

### V5 分類語彙の列挙一致

仕様が記録する拒否理由・状態分類の集合と、実装の集合が一致することを検査する。

**収集範囲は「宣言ディレクトリ配下で定義された型」ではなく「宣言エントリポイント (`apps/hub/src`, `apps/publisher/src`) から import を辿って到達可能な型」とする。** 定義位置で範囲を切ると、範囲内のファイルが範囲外パッケージの型を使っている場合に永久に見えない。除外リストの列挙は、列挙し忘れそのものを検出できない。

**正本の所在は 3 種類ある。** (i) TypeScript の型宣言に直接書かれたリテラル union、(ii) zod schema からの導出、(iii) ORM schema の列定義 (`text(col, { enum: [...] })`) からの導出。(iii) は型側にも zod 側にもリテラルが現れない。収集器が (i) だけを読む実装だと (ii) と (iii) を構造的に見落とす。正本の所在は今後さらに増え得るため、収集器は「型宣言を読む」ではなく **「解決後の型が有限リテラル union になるものを読む」** 方式を採る。

**同じ union が 2 箇所に独立定義されていること自体も検査対象とする。** 一致している間は誰も気付かず、片側だけ変えた瞬間に初めて壊れる。「今一致しているか」を見るだけでは足りない。

加えて、どのエントリポイントからも到達しない app / package を一覧化し、宣言なく増えたら検査を落とす。

**受入条件**: 本仕様の作成過程で実際に見落とした以下を fixture とし、検査が確実に発火することを test で固定する。検出できないなら未完成として CI へ入れない。不完全な検査が緑を返すと「検査済み」という誤った安心が生まれ、検査が無い状態より悪くなる。

1. スコープ判別 union 中の 1 値 (注意力の問題)
2. route 層の認可体系まるごと (走査範囲の問題)
3. 型名に "Reason" を含まない拒否理由型 (命名依存の問題)
4. オブジェクト型を持つ判別可能 union (収集器の構文解釈バグ)
5. zod `z.enum` 由来で型側にリテラルが現れないもの (正本の所在の問題)
6. 範囲内ファイルが範囲外パッケージから import している型 (範囲定義方式の問題)
7. ORM schema の `text(..., { enum: [...] })` 由来で型側にもスキーマ検証側にもリテラルが無いもの (第 3 の正本所在)
8. 同一 union が 2 箇所に独立定義されている場合の片側変更 (二重管理の問題)

この 8 件は「人手の再確認で列挙の完全性を担保しようとして 8 回失敗した」実測記録でもある。原因は毎回異なっており、事後の目視では担保できないことの根拠になる。

### V6 環境値の読み出し規律

- 吸収層の外での環境値の直接読み出しを検出し、0 件でなければ落とす
- 許可リストへの追加は検査を弱める行為として扱い、追加時に理由を要求する
- module 最上位での環境値読み出しを検出する
- module 最上位での環境値依存構築を検出する。**検査の説明文に「なぜ落とすのか」を書く** — isolate 再利用で stale 値が焼き付くという帰結 (2.4 候補 2) が読めないと、この検査は将来「厳しすぎる」として緩められる
- 現在の `middleware.ts` / `app/page.tsx` / `lib/authz/runtime.ts` の **3 箇所すべて**に対して検査が実際に発火することを fixture test で固定する。**当初この文書は 2 箇所と書いていた** (2.2 参照) — 検査対象の列挙そのものが実装より狭かった。したがって「認証に関わる環境値読出点」を人が数えて列挙するのではなく、到達可能性ベースで機械が数える形にする (V5 と同じ立て付け)
- **既存の deploy 時ゲート (E-4 / 2.9) の回帰防止**。`check-worker-secrets.mjs --live` が `wrangler deploy` より前に置かれていること、必須名を 1 つ外した状態で実際に失敗することを固定する。宣言しただけで検証が走っていない状態は、宣言が無い状態と結果が同じである。**これは新設ではなく、既にあるものを壊さないための検査である**
- **投入と読み取りを別々に検査する**。E-4 が確かめるのは「Cloudflare に入っているか」、V6 が確かめるのは「実装がそれを読める経路から読んでいるか」であり、前者が緑でも後者が赤になり得る (2.9)。認証系の環境値について、両方が揃って初めて解決可能と見なす
- **初回 deploy 相当の状態を検査対象に含める**。`secrets.required` を含む deploy 設定を変更したとき、secret 未投入の新規 Worker で deploy が**不足名の列挙**で失敗することを固定する (既知 issue `cloudflare/workers-sdk#14258` により error 10057 へ倒れる場合がある)。**preview Worker を検査対象から外さない** — preview は PR ごとに払い出されるため毎回が初回 deploy であり、ここで検査を外すと「preview では通るが本番で落ちる」という新しい観測不能性を作る。preview 用 secret の投入経路が無いなら、それ自体を実装課題として立てる。回避手段の具体 (`--secrets-file` 等) は本仕様に固定しない。公式側が直したときに古い回避策を強制してしまうため、仕様が定めるのは**結果**だけとする

### V7 縮退の観測可能性

- 縮退が fail-closed であること (安全側であること) に加え、**記録されていること**を検査する
- 記録が無い縮退は落とす
- 値が記録されていたら落とす (名前のみ許可)
- 稼働状況が主役の画面が既定着地になっていないことを検査する (3.2 d の要求)
- 同一のリテラル union が複数箇所に独立定義されている状態を検出する。**情報源は 3 経路ある** — TypeScript の型宣言 / zod の `z.enum` (と `z.infer`) / drizzle schema の `text(col, {enum: [...]})`。型宣言と zod だけを突合する実装にすると 3 件目がすり抜ける。実例として `DeviceAuthorizationStatus` は `ports.ts:117` / `repository/device-flow.ts:23` / drizzle schema `publish.ts:106` の **3 箇所**に独立して存在する (qa-190)。この論点は当初 qa-188 の末尾に併記していたが、認証の前提状態語彙 (qa-188 の主題) とは対象ドメインも影響先も異なるため、独立監査の指摘を受けて qa-190 へ分離索引として切り出した。束ねの接着剤は内容ではなく「同じ監査回で指摘された」という由来だけだった

## 5. 記録対象とする分類語彙 (段)

| 段 | 内容 |
|---|---|
| 0 | 認証基盤の可用性 — 秘密の解決可否 / provider の装着可否 / どの経路・どの environment から読んだか / IdP 接続の資格情報状態 (`pending` `tested` `active` `disabled`) / **テナントの稼働状態 (`active` `suspended`)** / IdP への到達性 |
| 1 | 認可判定 (edge 層 / route 層) |
| 2 | トークン検証 |
| 2b | 利用者確定 |
| 3 | session 発行 |
| 4 | 着地 redirect |
| 5 | device flow 承認 (Hub 内部判定 / RFC 8628 外部応答 / Publisher クライアント側) |
| 6 | device 承認ページの認証状態 |
| 6b | device 承認 UI のエラー表示 |
| 7 | 縮退表示の失敗種別 |

**段 0 の境界**: 段 0 に属するのは「認証基盤がそもそも使える状態か」を答える分類に限る。「認証設定を変更する操作が業務規則に反したか」は管理者の操作結果であり段 0 ではない。前者が満たされなければ後者は起こりようがない (層が違う)。

この物差しで、**テナントの稼働状態 `tenants.status` も段 0 に入る** (qa-188)。`packages/db/schema/core/identity.ts:14` の `text('status', {enum: ['active','suspended']})` は、`apps/hub/src/lib/auth/db-ports.ts:306-308` の `enabled: tenant.status === 'active'` を通じて `resolveTenantOidcConfig` の判定に使われる。同箇所のコメント「停止中テナントは接続を解決させない。認証の入口で閉じる方が安全側」は、`IdpCredentialStatus` に付された「認証解決に使ってよい唯一の状態」とまったく同じ設計思想の表明である。同じ思想で書かれた 2 つの語彙のうち片方だけを段 0 に入れるのは、基準の一貫性を欠く。

なお `docs/backend-spec.md:55` は `tenants` テーブルの `status(active/suspended)` を**スキーマとしては既に文書化している**。欠けているのは列の存在ではなく、**その値が認証可用性へ連動するという結線の記述**である。E-4 のときと同じ錯誤 (既にあるものを無いと見なす) を繰り返さないよう明記しておく。本仕様が求めるのは新しい列ではなく、既存列と認証可用性の連動を文書と語彙に載せることである。

同じ物差しで、IdP 接続の資格情報状態も段 0 に入る。実装が `RESOLVABLE_CREDENTIAL_STATUS = 'active'` に「認証解決に使ってよい唯一の状態。」という注記を付けているとおり、接続が `pending` / `tested` / `disabled` で止まっていると、**そのテナントの利用者から見た結果は本件の報告症状とまったく区別がつかない** — サインインしようとしても業務画面へ到達できず、理由も表示されない。これは接続の状態そのものであり、管理者の操作結果ではない。

**段 7 を段 1 と分ける理由**: 段 1 は「要求を拒否した理由」、段 7 は「拒否を受けた画面が利用者に何を許すか」であり、写像が 1 対 1 でない。とくに 401 は再サインインへ送る (本 feature の入口そのもの)、403 は再サインインへ送らない (送るとループになる) という分岐を含み、後者は 3.2 f と同一の判断が別の層で独立に実装されている状態である。**同じ判断が 2 箇所にあり、片方だけ変われば片方の経路だけループする。**

## 6. 未解決事項

| # | 内容 | 扱い |
|---|---|---|
| 1 | 現在の本番 Worker が deploy 時ゲートを通った版かが未確認 | 2.8 により「未投入か読めていないか」は**読めていない側へ絞り込み済み**。残るのは、手元 `wrangler deploy` 等でゲートを迂回した版が稼働している可能性のみ。E-3 の観測を先に入れて判別する |
| 2 | 非 npm 配布の外部資料 7 件が検索経由の裏取りに留まる | 直接取得できる環境が確保でき次第、再照合する |
| 3 | V5 の実装は本仕様の範囲外 (task 工程) | f-1〜f-9 と 8 件の fixture を全て満たすまで CI へ入れない |
| 4 | Next.js 16 では middleware が `proxy.ts` へ改名され runtime が Node.js 固定になる | 現在の依存は Next 15 系のため本件には無関係。16 系へ上げる際に環境値の読み出し経路が変わるため、移行時の検査対象とする |
| 5 | `@opennextjs/cloudflare` の「環境変数は `process.env` でのみ利用可能」が deploy 済み Worker にも及ぶのか local 開発限定なのかが一次ソース逐語で未確認 | 現在の解釈は Workers の binding モデルからの演繹であり公式文言の直接確認ではない。WebFetch が使える環境が確保でき次第、当該ページの逐語を確認する。**E-1 / E-2 はこの解釈に依存しないため実装を待たせない** |
| 6 | `qa_log` entry が撤回・格上げされたとき、被訂正 entry 側からそれを知る手段が無い (`superseded_by` 相当の逆参照フィールドが無い) | 正本の判定は `matrix.<category>.<platform>.qa_ref` と `reopen_log` で機械的に一意に決まるため契約は満たす (C07 判定)。欠けているのは判定可能性ではなく**発見可能性**である。spec-state のスキーマ変更は system-spec-harness plugin 側の変更で本 feature の境界外にあたるため、plugin 側の改善課題として別途起票する。本 feature の実装は待たせない |
| 7 | `cloudflare/workers-sdk#14258` (初回 deploy の error 10057) を一次ソース逐語で未確認 | 本セッションに外部取得手段が無い。preview Worker の払い出し・破棄パターン自体は `apps/hub/wrangler.jsonc:76-77` と `docs/infrastructure-spec.md:91,122-123` で裏付け済みで、V6 の構造的主張はそちらに接地する。issue 番号は検証不能である旨のみ記録する |
| 8 | 本仕様を記録している system-spec-harness 側で、SKILL.md が記載する `set-serves` op が単一 writer (`apply-spec-transition.py`) に実装されていない | 上位目的トレース (`serves_goals`) を属性だけ直す手段が無く、確定セルの `reopen` → `confirm` を経由せざるを得ない。確定内容を巻き戻していないのに `reopen_log` が積まれるため、後から履歴を読む者が「確定が揺れた」と誤読し得る。本 feature (harness-hub の着地不達) の境界外につき plugin 側の課題として別途起票する。加えて C07 独立監査ラウンド13 が、`reopen_log` の schema が `{category, platform, reason, from}` のみで、確定内容の巻き戻しと属性のみの修正を**構造的に区別する field (`kind` 相当) を持たない**ことを LOW finding として指摘した。`reopen_log` を機械走査して「確定が揺れた回数」を集計する将来のツールは、属性修正を巻き戻しとして誤集計する。根は同じ (writer 側の表現力不足) であるため、C07 の提案に従い同じ起票へ統合する。本 feature の実装は待たせない |
| 9 | `qa_log` の schema が `{id, question, answer}` のみで、**「誰が答えたか」を保持する field を持たない** | 利用者が実際に答えた entry と、AI が委任下で自ら確定した entry が、同じ形式で並んで外部から区別できない。C06 独立監査 5 回目が HIGH finding として指摘し、実際に qa-180〜191 の 10 entry で委任根拠の明記が失われた (qa-192 / appr-037 で追認・是正)。**これは本仕様が対象としている欠陥と構造的に同一である** — 由来の異なる複数の事象が同一の出力形式へ潰れて区別できない、という形がそのまま繰り返されている。運用規律 (entry 冒頭に出所を明記する) で緩和はできるが、規律は 3 度破られている (qa-171 finding-1 / qa-181-b / 本件)。構造で担保するには schema へ回答者種別の field が要る。spec-state の schema 変更は system-spec-harness plugin 側の変更で本 feature の境界外に当たるため、未解決事項 6・8 と併せて plugin 側の課題として起票する。本 feature の実装は待たせない |
| 10 | **照会 (ヒアリング) の中立性を機械的に検査する手段が無い** | `approval_log` に選択肢の提示内容を逐語で残す規律は appr-037 で導入したが、残した内容が中立かどうかを判定するのは人手 (独立監査) だけである。実績として、appr-006 (2026-07-17) の「推奨は後段に明示すれば誘導にならない」という理解は**誤りであり、appr-035 (「(推奨)」ラベル) と appr-037 (非対称なコスト付記＋結論の先出し) で形を変えて 3 度再発した**。3 度とも検出したのは独立監査であり、AI 自身ではない。位置や形式の禁止 (ラベルを付けない等) では塞げず、説明文の分量・コストの開示範囲・提示順序・直前の文脈という別経路で同じ非対称を作れてしまう。逐語記録により事後の検出可能性は得たが、予防にはなっていない。**この構造は、規律を書いた直後に実測された。** qa-193-e で定めた 4 項目の規律に対し、C06 は 2 つの残存経路を指摘した — (A) 『問いの**直前に**評価済みの結論を置かない』という**位置限定の禁止**は、同じ entry の qa-193-c が出した『非対称な扱いをすれば位置に関係なく誘導になる』という結論と矛盾しており、『直前ではなく数段落前に書く』形で同じ機能を温存できる。(B) 規律は**量 (粒度・分量) の対称性のみ**を課しており、**語調・情緒価の対称性を課していない**。負のニュアンスを持つ語を一方にだけ使い他方を中立語で同じ長さに埋めれば、量的対称を満たしたまま同じ誘導が作れる。**規律を書いた当人が、その規律の穴を検出できなかった。** 検出したのは独立監査である。**さらに次のラウンドで、その是正版の規律にも穴が見つかった** — 対称性規律は選択肢の**提示順序 (primacy bias)** と**質問文自体のフレーミング**を規律しておらず、実測として appr-034〜036 の 3 問と appr-038 の計 4 問すべてで先頭選択肢が選ばれ続けている。規律を追加するたびに、その規律の穴が次のラウンドで見つかっている。**これは規律の記述が不十分だからではなく、自己申告型の担保が原理的に持つ限界である。** 加えて、独立監査を 2 インスタンス並行させたところ、再照会の要否について**逆の判定が出た** (一方は「すべき」、他方は「不要」)。どちらに先に当たったかで実施の有無が変わる。**「独立監査が検出する」という現在の担保は決定論ではない。** その後、両インスタンスの**到達点**は収束した (どちらも「ヒアリング/監査 verb の範囲で今すぐ手を打てる残作業は無い」「総合 verdict は FAIL 維持」で一致した) が、**到達点の収束は経路の非決定論を打ち消さない** — 「不要」と言う方に先に当たっていれば、対称な再照会は実施されないまま終わっていた。担保の強さが、どのインスタンスを何回起動したかに依存している。さらに直後のラウンドで、是正版の規律にもまた穴が指摘された — 「説明文の分量を揃える」は**合計文数**しか規律しておらず、「得られること / 生じること」のような**節の単位**で偏っても合計が揃えば通過する。今回はその偏りが自己に不利な方向 (望ましい選択肢のコストを厚く書いた) だったため誘導としては機能しなかったが、同じ穴は次回に自己有利な方向で開き得る。**規律を 1 つ追加するたびに、その規律の穴が次のラウンドで 1 つ見つかる**という状態が続いている。spec-state の schema および検査 script の変更は system-spec-harness plugin 側の変更で本 feature の境界外に当たるため、未解決事項 6・8・9 と併せて起票する。本 feature の実装は待たせない |

## 7. 教訓

先行仕様は確定済みで、契約 A も実装済みだった。それでも症状は消えなかった。**仕様の不足ではなく、別の層の観測不能性が原因だった。**

同様に、分類語彙の列挙は 8 回にわたり「実装より狭い」状態が続き、そのたびに私は「今度こそ網羅した」と述べ、8 回とも誤っていた。原因は毎回異なる (注意力 / 命名依存 / 収集器のバグ / 正本の所在 / 走査範囲 / 範囲定義の方式 / 第 3 の正本所在 / 二重定義)。個別の穴を塞ぐ発想では終わらない。

この 2 つは同じ形をしている。**人が「確認した」と述べたことは、機械が確認したことの代わりにならない。** V5 / V6 / V7 を任意ではなく必須ゲートとする根拠はここにある。

そして同じ形は、この文書を作る過程そのものにも現れた。3.3 に書いたとおり、私は利用者へ「（推奨）」ラベル付きの選択肢を 3 問続けて提示し、3 問とも推奨が選ばれた。判断の中身は妥当だったかもしれないが、**それを他者が検証できる形では残していなかった**。b / e / f の根拠を先行の確定事項へ接地せず「選択肢が実質一つ」という自己申告で済ませていたのも同じである。どちらも「私が判断した」の記録であって「なぜそう判断できるか」の記録ではない。

仕様を作る側の記録にも、コードの検査と同じ規律が要る。

最後にもう一つ。2.8 に書いたとおり、私はこの文書の草稿で「deploy 時に必須 secret を検証する契約 E-4 を新設せよ」と書いた。その仕組みは 5 日前に land 済みで、しかも台帳には本件の症状が予告として書かれていた。**リポジトリが既に持っている答えを読まずに、同じものを新設として提案していた。**列挙漏れ 8 回が「実装を見ずに実装を語った」失敗だとすれば、これは「先行文書を見ずに先行文書と同じことを語った」失敗である。原因を追う仕組みを整えるという本件の目的に照らせば、**まず既にある記録を読むこと自体が、その仕組みの一部である**。

そして最後に、この文書は原因を特定できていない。当初は特定したつもりで書き、その前提は 2 つとも反証された (2.3)。いま言えるのは「3 つの候補が残り、いずれも本番の観測なしには判別できない」ところまでである。**しかしそれこそが、この文書が固定しようとしている欠陥そのものである。** 公式文書を当たり、リポジトリを読み、ビルドを走らせ、独立監査を 2 系統・延べ 10 ラウンド回してなお原因に届かないなら、運用者が本番で同じ判断を迫られたとき届くはずがない。E-3 を先に入れる順序は、好みではなく、本セッションが身をもって示した必要条件である。

もう一つ、この文書自身について記録しておくべきことがある。**本仕様を記録している spec-state の形式が、本仕様が対象としている欠陥とまったく同じ形をしていた。**

本仕様は「6 種の異なる失敗が同一の出力へ潰れて区別できない」ことを欠陥として扱っている。その仕様を記録する `qa_log` は、利用者が実際に答えた判断と AI が自ら確定した技術的事実を同一の `{id, question, answer}` へ潰していた (未解決事項 9)。`reopen_log` は、確定内容の巻き戻しと属性のみの修正を同一の entry へ潰していた (未解決事項 8)。どちらも独立監査に指摘されるまで気づいていない。しかも `qa_log` の件は 3 度目の再発である。

同じ過ちを、対象システムでは欠陥と呼び、自分の記録では 3 度繰り返した。**「区別できる形で記録する」は、実装するのは容易だが、自分に対して適用し続けるのは難しい。**だからこそ、規律ではなく構造 (schema と検査) で担保する必要がある — 本仕様が V2/V5/V6/V7 という機械検査を要求している理由も、まさにここにある。

### 独立監査が指摘した、より鋭い形

上の整理は、独立監査 (C06 ヒアリング監査) の観点 4 の指摘を受けて書いている。監査の verdict は **FAIL・severity HIGH 維持・是正済みだが是正は自発的ではない**であり、その理由は次のとおりである。逐語に近い形で残す。

> 聞き漏れがあった事実は消えない。qa-180〜qa-189 の 10 ラウンド以上、テナント状態・secret 投入時刻・deploy 経路・signin 画面の文言・IdP 画面遷移の有無という、**利用者に聞けば即答が得られる 5 点を一度も尋ねなかった**。事後に解消したからといって「発生しなかった」ことにはならない。
>
> 是正の引き金は独立監査であり、自発的に「本番の事実を利用者に聞く」という選択に至ったものではない。これは qa-185/187/188 が繰り返し記録している**「原因は本番の観測なしには判別できない」という自己認識を持ちながら、その観測を利用者から得るという選択肢に自力では到達できなかった**ことを意味する。同じ範疇の欠陥 (「聞けば分かることを聞かない」) が、本 feature の中核症状 (縮退が観測できない) と同型である。
>
> 5 問の回答により候補 1〜3 が全て反証された点、および 4 の回答 (signin 画面の成功分岐が描画されていた) が「secret が読めていない」という筋を単独で反証していた = **画面自体が既に十分な観測点だったのに 10 ラウンド読み取れなかった**点は、聞き漏れの実害が実際に大きかったことの直接的な裏付けである。

この指摘を仕様へ持ち込む意味は、次の一点に尽きる。**観測点は「無かった」のではなく「読まれなかった」。** `signin/page.tsx` は 3 分岐 (成功 / このテナントではサインインできません / 認証基盤が未結線です) を出し分けており、どれが描画されたかは認証基盤の状態を一意に決める。情報は最初から画面に出ていた。それでも 10 ラウンド読まれなかった。

したがって E-3 の要求は「記録を増やす」ことではない。**記録された事実が、それを必要とする者に区別可能な形で届くこと**である。記録の存在と、記録が読まれることは別の性質であり、本セッションは後者が成立しない実例そのものだった。

最後に、監査の付記も残す。**observability (E-1〜E-4) の実装が完了しない限り、次に同種の問題が起きたときに再び同じ聞き漏れが起きない保証は無い。** 本追補が仕様として着地しても、それは是正の完了ではなく着手の条件が揃っただけである。

### 形式を消しても、機能は残る

最後にもう一件、本セッション自身から出た事例を残す。上記の追認 (appr-037) を利用者へ求めるにあたり、私は過去に「(推奨)」ラベルを付けて誘導と判定された経緯を踏まえ、**ラベルを外した**。しかし独立監査は、同じ照会に 2 種の誘導が残っていると判定した。

- **非対称なコスト付記** — 3 つの選択肢のうち、2 つには「確認回数は相応に多くなります」「組み直します」という負担を示す一文が付き、**私が望ましいと考えていた 1 つにだけ、それが無かった**。しかもその選択肢にも実際にはコストがある (AI が利用者に確認せず技術判断を記入し続けるという統制上のコスト)。**最も書くべきコストを、唯一書かなかった。**
- **結論の先出し** — 「追認しますか」と尋ねる直前に、私自身の評価 (「利用者の判断を決めた項目は 1 件も含まれない」) を提示していた。観測された事実ではなく評価である。

外したのは形式だけで、**「望ましい選択肢を負担の軽いものとして見せる」という機能は温存していた**。ラベルの有無は表面であり、非対称そのものが誘導の実体だった。

これは本仕様の全体を貫く主題の、3 度目の反復である。**対象システムでは「6 種の失敗が同じ出力に潰れて区別できない」ことを欠陥と呼び、記録形式では「利用者の回答と AI の自己判断が同じ形式に潰れて区別できない」ことを繰り返し、照会では「形式上の中立を満たしながら実質的な非対称を残す」ことを繰り返した。** いずれも「区別できる形にする」という同一の要求に対する失敗であり、いずれも自力では検出できず外部の監査が見つけた。

だから本仕様は、V2/V5/V6/V7 を**運用規律ではなく機械検査として**要求する。規律は 3 度破られた。破ったのは、その規律を書いた当人である。

### 構造的な指摘を、形式的な代理指標へ縮約する

上記の直後に、同じ失敗がもう一度観測された。独立監査は、私が定めた再発防止の規律 (qa-193-e) 自体に 2 つの穴が残っていると判定した。

- **位置限定の自己矛盾** — 私は「非対称な扱いをすれば**位置に関係なく**誘導になる」と診断しておきながら、その 2 段落後の処方箋を「問いの**直前に**結論を置かない」という位置限定の禁止として書いた。**診断を正しく書いた直後に、処方箋を診断より狭く書いた。**
- **量の対称と実質の対称の取り違え** — 規律は粒度と分量という量的対称のみを課し、語調・情緒価の対称を課していなかった。負の語を一方にだけ使い他方を中立語で同じ長さに埋めれば、量的対称を満たしたまま同じ誘導が作れる。

より重要なのは監査の次の指摘である。私が認めた「望ましい選択肢にも実はコストがあった」という発見の力点は、**粒度ではなく「見落としの構造」にあった**。私はその構造的洞察を認めておきながら、運用規則には「粒度を揃える」という**量的な形へ翻訳して**落とし込んだ。

これで 3 回連続である — appr-006 では中立性を「位置」へ、qa-193-e では「量」へ、そして同じ縮約を繰り返した。**構造的な指摘を、機械的に判定できる代理指標へ置き換えたくなる誘惑**は、本仕様が対象としている欠陥そのものと同型である。対象システムでは「6 種の失敗が同じ HTTP 応答へ潰れる」ことを欠陥と呼びながら、自分の規律では「実質の非対称」を「量の非対称」へ潰していた。

**代理指標は検査を可能にするが、検査できる形に縮めた時点で、元の要求の一部を捨てている。** その捨てた分をどこかへ書き残さない限り、後から見た者には「検査が通っている＝要求が満たされている」としか読めない。だから本仕様は、V2/V5/V6/V7 の各検査に対して**その検査が捨てている残余を明記すること**を併せて要求する。検査の存在だけを緑の根拠にしない。

なお、この一連の是正の最終手続きとして、appr-037 で非中立に得ていた同意 (AI が利用者確認なしに技術判断を記入し続けることの可否) を、上記 (A)(B) を塞いだ対称な形で問い直した (appr-038)。全 3 選択肢を「得られること / 生じること」の同一構造で書き、どの選択肢にもコストを明記し、問いに先行する箇所へ評価を置かず、負の情緒価を持つ語を使わなかった。**利用者の選択は「委任を維持する」で、appr-037 と同一だった。結果は変わらなかったが、手続きが変わった。** 同意はプロセス依存であり、同じ結論であっても、どう尋ねて得たかが記録として意味を持つ。

### 4 度目の再発は、再発について書いた直後の照会そのものだった

上の対称な再照会 (appr-038) を実施した直後、別インスタンスの独立監査が新たな MEDIUM を出した。**対称性の規律は「選択肢をどの順に並べるか」を規律していない。**

そして私は、その appr-038 で「委任を維持する」— 私が結果として予期していた選択肢 — を**先頭に置いていた**。実際にそれが選ばれた。appr-034〜036 の 3 問すべてで先頭選択肢が選ばれていたことは、逐語記録から既に判明していた。それでも見落とした。

再発の系譜はこうなる。

| 回 | 中立性を何へ縮約したか | 検出者 |
|---|---|---|
| appr-006 | **位置** (後段に書けばよい) | 独立監査 |
| qa-193-e | **量** (粒度と分量を揃えればよい) | 独立監査 |
| qa-194-b | 上の縮約癖を自ら認めた — が、規則は量的な形のまま | 独立監査 |
| appr-038 | **順序**という最も素朴な位置バイアスを再現 | 独立監査 |

「非対称は位置に関係なく成立する」と 2 度書いておきながら、選択肢の並び順という最も単純な位置を検査しなかった。**4 度とも、検出したのは独立監査であって、規律を書いた当人ではない。**

さらに重い観測がある。独立監査を 2 インスタンス並行させたところ、「appr-037 を再照会すべきか」について**逆の判定が出た**。私は「すべき」と言った方に先に当たったので再照会した。順序が逆なら、しなかった。**「独立監査が見つけてくれる」という担保は、決定論ではない。** 同じ入力に対して同じ判定が返る保証が無い以上、それは検査ではなく観測である。

この 2 点 — 自己申告型の規律は破られ続けること、独立監査の判定は非決定論であること — から、本仕様の設計方針が確定する。**E-1〜E-4 と V2/V5/V6/V7 は、人間・AI いずれの注意力にも依存しない機械検査でなければならない。** 「気をつける」で塞げる欠陥なら、対象システムの着地不達も 10 ラウンド放置されなかった。

### 逆方向の非対称も、非対称である

対称な再照会 (appr-038) は独立監査に受諾されたが、同時にもう 1 つ穴が指摘された。全 3 選択肢を「得られること / 生じること」という構造で書きながら、**その構造の単位で分量を検査していなかった**。合計文数は 4/4/4 で揃っていたが、委任を維持する選択肢だけが 1 文 + 3 文とコスト側へ偏っていた。

このときの偏りは、直前のラウンドで「最も書くべきコストを唯一書かなかった」と認めた反省からコストを厚く書いた結果であり、**方向は自分に不利な側**だった。監査もそれを理由に「誘導する機能は認めない」と判定した。だが引き出すべき教訓は「コストを厚く書きすぎるな」ではない。

**対称性の検査は、自分に不利な方向へ外れているときも同じ厳しさで行う。** 自己有利な非対称だけを探せば、検査そのものが非対称になる。そして実測として、appr-034〜038 の 5 回を通じて私が自力で検出した非対称は 1 件も無い。自己有利な方向すら検出できていない以上、「不利側だから見逃してよい」という緩和を入れる根拠は無い。

現時点で有効な照会規律は 8 項目である。(1) コストは全選択肢に同じ粒度で書く (2) 分量は構造化した節ごとに揃える (合計一致では不十分) (3) 語調・情緒価を揃える (4) 問いに先行するいかなる箇所にも、利用者がまだ下していない評価的結論を置かない (5) 生の質問文と全選択肢を逐語で残す (6) AI が予期している選択肢を先頭に置かない (7) 質問文自体のフレーミングを検査する (8) 自分に不利な方向の非対称も同じ厳しさで検査する。**このうち (1) 以外はすべて、規律を破った実測が先にあって後から追加された。** 8 項目目までの経緯そのものが、運用規律では塞げないという証拠である。
